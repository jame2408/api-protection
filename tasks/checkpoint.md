# Checkpoint

> 唯一續接入口（`docs/adr/adr-013-content-tiering-and-injection-slimming.md` 決策 (c)）。欄位比照 `tasks/_templates/checkpoint.md`；歷史交接紀錄見 git log（本檔內容取代 `tasks/archive/process-improvement-plan.md` §8.5 原文）。新 session 直接讀本檔即可接手，不需要先讀 plan 全文。

---

## 分支

`main` only（TBD 轉換已完成；分支紀律見 `docs/adr/adr-012-charter-amendments-external-adoption.md` 決策 (e)）。

## 已完成（含 commit hash）

> 本欄依「如何接上」條文只保留最近數項；更早紀錄（TBD 轉換、Phase I–K、Wave 1 全 10 場景、Wave 2 全 7 場景、ADR-012～026 落地、防線機械化各包、上游 skill charting 與落地、lessons triage 一～四輪、Fable 5 交接包）見 git log 與 `tasks/archive/`。

- **scenario「Security Admin 解鎖金鑰」真 Red→Green，31/46 — C4 UnlockKey 新切片開場** — 派工前勘查四點閉合：§3.2.7 無 request body（鏡射 Resume `PostAsync(url, null)`）；Errors 表與場景 5 對齊、FORBIDDEN 屬場景 6 契約缺口本輪不涉；Authorization 欄 SecurityAdmin-only 明文，但 role policy 依 ResumeKey `90feb44`／LockKey `789e562` 先例留待「操作者權限不足」場景紅驅動——endpoint 用 bare `RequireAuthorization()`＋deferred rationale 註解；Given×2／Then「狀態變為 Active」三個既有 step 直接復用。C4 垂直切片鏡射 ResumeKey 全套：`ApiKey.Unlock(Actor)`＋`KeyUnlocked(keyId, unlockedBy: Actor)`＋Handler 兩 guard（exists→404、非 Locked→409，沿用 §2.2 共用字面值、ApiProblem.Map 零新條目）＋Endpoint `POST …/unlock`＋DI/Map。新 steps 恰 2 個（When 解鎖命令不重發 token；Then KeyUnlocked outbox nested Actor＋`reason` 缺席 wire-shape 鎖定，鏡射 KeyResumed）。真紅 A（恰 2 undefined steps）＋真紅 B（暫緩 Module 註冊，404）→ 綠 36/15，Architecture.Tests 14/14；`UnlockKeyHandler` coverage 87.5%（full gate 口徑 86.7%，皆 ≥80%）— `f1e66b2`。executor 零 blocker、friction 2 條良性（Edit 前須以 Read 工具重讀曾用 cat 讀過的檔；coverage 輸出目錄 /tmp 被 harness 拒改用 scratchpad）（129.8K tokens／60 calls／6.8 分，standard-code 路由）；Refactor-assessment：兩側均判不動——PostAsync null body 三行樣板第 2 次出現，併入既有 test-only pass 候選觀察。orchestrator 親驗 scope／trailer／semantic review 後放行 push（pre-push full gate 綠）
- **scenario「金鑰非 Locked 狀態 — 拒絕解鎖」Red→Green，32/46** — 純 test-only 啟用（`UnlockKeyHandler` guard 2 本就在 `f1e66b2`）：勘查求證 TestHooks 預設 token 即 SecurityAdmin（`TestHooks.cs:97–103`，ADR-024 §5 註解），場景無 actor Given 直接依賴預設；唯一新 step 為 Then「解鎖失敗」對照表（鏡射「鎖定失敗」先例，僅一條 INVALID_STATE_TRANSITION/409；場景 6 FORBIDDEN 未列 §3.2.7 Errors 表＝契約缺口，註解明文不預鎖）。自然紅（恰 1 undefined step）→ 綠 37/14 → 故意紅（seed Active→Locked，409 斷言收 200）→ cp 快照還原（md5 比對一致）回綠，production 零改動（`git diff --stat backend/src/` 空）；`UnlockKeyHandler` coverage 86.7%→93.3%（guard 2 負路徑）— `248acc3`。executor 零 blocker、friction 2 條良性（coverage-check 需自組 results-dir 呼叫慣例；複合指令被權限系統擋拆單行過）（90.7K tokens／66 calls／5.5 分，standard-code 路由）；Refactor-assessment：「X失敗」對照表樣板第 4 次出現，維持現狀交 orchestrator 覆核。orchestrator 親驗 scope／trailer／semantic review 後放行 push（pre-push full gate 綠）
- **scenario「操作者權限不足 — 拒絕解鎖」真 Red→Green，33/46 — C4 UnlockKey 收官、04 檔 6 場景全綠** — SecurityAdmin-only role policy 兌現（先例套用免裁決：機制已 Resume `90feb44`／Lock `7f02530` 兩度落地；api-spec §3.2.7 Errors 補 FORBIDDEN 列屬機械同步走同 commit）。production：`UnlockKeyEndpoint` bare auth → `RequireRole("SecurityAdmin")`（**不含 TenantAdmin**——§3.2.7 明文，解鎖是資安人工確認語意，與 ResumeKey 的差異 rationale 刻在 endpoint 註解）。test：Given「操作者為一般 Consumer」attribute 疊加零新方法、新 When（Consumer token 不重發＋Locked seed 隔離證明拒絕來自 role policy）、對照表補 FORBIDDEN/403 並改寫預告註解。真紅 A（恰 2 undefined）＋真行為紅 B（403 斷言收 200）→ 綠 38/13，回歸紅線（場景 4／5 SecurityAdmin token 過 policy）逐場景綠證，coverage 93.3% 持平 — `b6e2ebf`。**含一次退回件**：semantic review 發現新 When 方法誤置 `// Then` 分區線後，退回 executor 整塊搬移＋amend（內容零改動），複驗落點後放行；另 friction 2 條良性（api-spec Edit 前未 Read 被擋；對照表註解第一版疊字失誤自行修正未進 commit）（114.3K＋amend 輪 tokens／79 calls／5.0 分，standard-code 路由）。orchestrator 親驗 scope／trailer／semantic review 後放行 push（pre-push full gate 綠）
- **佇列勘誤＋scenario「成功啟動金鑰輪替」真 Red→Green，34/46 — C2 RotateKey 新切片開場（Wave 3 最大切片）** — **開場勘誤**：前輪 spec 以 CLAUDE.md 純字典序 grep 判定下一場景，把 05 檔 line 5 首場景（`:5` 字典序排在 `:19` 後）誤判為已綠，錯誤指標進帳面——feature 原文覆核現形，勘誤 `843b9f3`＋lesson `20260712-ignore-grep-lexicographic-sort-trap.md`（佇列查找一律用 bdd-progress.md 記載的 `sort -t: -k1,1 -k2,2n`）。**三項使用者裁決**（2026-07-12）：Key B `expiresAt` 繼承 Key A（輪替只換憑證材料）／gracePeriod 預設 24 小時（api-spec §3.2.4 補值同 commit）／Authorization 本輪 bare auth＋缺口登記（見待裁決欄）。切片：`ApiKey.GraceDeadline` 欄＋migration `AddGraceDeadlineColumn`（僅一欄，已核實）＋`InitiateRotation(successorKeyId, gracePeriod, now)`（單方法合併轉移與 successor——INV-2 結構性保證，rationale 刻 doc-comment）＋`SetPredecessor`＋`KeyRotationInitiated` 事件（wire 三欄對齊 integration-spec L516）＋Handler（guard 2 道：exists／非 Active；已到期與 INV-4 兩 guard 留場景 36/37 紅驅動，deferral 註解含插入位）＋巢狀 response（oldKey 4 欄＋newKey 12 欄）＋endpoint ISO8601 duration 以 `XmlConvert.ToTimeSpan` 邊界解析。test：`RotateKeySteps.cs` 新檔 10 steps（graceDeadline 以 FrozenTimeProvider 精確相等斷言；outbox 以 EventType＋AggregateId 匹配）＋Given「尚未到期」attribute 疊加。真紅 A（11 undefined）＋真紅 B（endpoint 未註冊空 body）→ 綠 39/12，Architecture.Tests 14/14；`RotateKeyHandler` coverage 93.9% — `9e0e432`。executor 零 blocker、friction 3 條（Cucumber Expression `\+` 非法跳脫致 binding registry 全滅 34 failed，改字面 `+` 復原；`dotnet ef` 參數組合自 migration 檔頭推得一次成功；scratchpad `rm -rf` 被擋改 `mkdir -p`）（185.7K tokens／158 calls／14.9 分，standard-code 路由）。orchestrator 親驗 scope／trailer／semantic review（production／migration／steps 逐檔）後放行 push（pre-push full gate 綠）
- **RotateKey 授權缺口收口 — ADR-022 §3 缺陷再現 ×2＋CLAUDE.md 佇列指令修訂（2026-07-12 使用者核准「補 ADR-022 分流與修訂 grep 指令」）** — (0) CLAUDE.md 佇列查找指令改數值排序（與 bdd-progress.md SSOT 對齊）＋lesson 落地欄同步兌現 — `f8a2e2e`。(1) 缺陷再現 1/2「操作者無輪替權限 — 拒絕輪替」：場景不帶 @ignore 直接落（§3 豁免凍結與晉升），再現紅（403 斷言收 200，SecurityAdmin 過 bare auth 完成輪替）→ `RotateKeyEndpoint` 落 `RequireRole("TenantAdmin", "Consumer")`（§3.2.4 逐字）→ 綠 40/12；api-spec Errors 補 FORBIDDEN 列同 commit，`Spec-change: defect-repro` trailer — `fceed32`（87.4K／49 calls／5.8 分）。(2) 缺陷再現 2/2「Consumer 輪替非自身金鑰 — 拒絕」：ownership 半邊——endpoint 讀原始 `consumerId` claim（MapInboundClaims=false 已核）→ handler ownership guard（exists 後、status 前；claim 缺席＝TenantAdmin 不受限，rationale 刻註解）＋`RotateKeyFailureCodes.Forbidden` 沿用共用字面值零新 Map 條目；**同 commit test 對齊**：場景 34 的 When 改發 `CreateConsumerToken(consumerId: "any-consumer")` 對齊 seed（否則 guard 落地即誤紅——spec 預核，步驟 2 含「34 轉紅即停止」防呆，實測 34 全程綠）；新 Given 以 `"other-consumer"` seed 給出「非自身」機械定義。再現紅→綠 41/12，`RotateKeyHandler` coverage 94.2% — `99d984c`（142.3K／81 calls／8.3 分）。兩輪帳面分母 46→48（§3.4），佇列「下一個」指標不受影響；兩輪均零 blocker、friction 皆良性（detailed verbosity 才印 passed 場景名——已回饋進下輪 spec 指令；複合指令權限拆單行）。orchestrator 親驗 scope／trailer／semantic review 後一次放行 push（pre-push full gate 綠）；security review 標紅項（IDOR／跨租戶接管）就此收口
- **scenario「金鑰非 Active 狀態 — 拒絕輪替」Red→Green，37/48** — 純 test-only 啟用（status guard 本就在 `9e0e432`，現為 guard 3）：唯一新 step 為 When「Consumer 對 "(.*)" 發起輪替」（token 發 `CreateConsumerToken(consumerId: "any-consumer")` 先過 role policy＋ownership guard 才命中 status guard——guard 鏈核對 spec 預載；body `Request(null)` 順帶覆蓋 gracePeriod 預設 24h 路徑）＋對照表補 INVALID_STATE_TRANSITION 條目（非新 step）。自然紅（恰 1 undefined When）→ 綠 42/11 → 故意紅（seed Suspended→Active，409 斷言收 200）→ cp 快照還原（md5 一致）回綠，production 零改動；`RotateKeyHandler` coverage 94.2%→96.2% — `6145696`。**含一次退回件**：對照表新條目註解誤寫「Errors 表已補列（本 commit）」（自 FORBIDDEN 條目鏡射誤帶，該碼實為既有列），退回 executor 一行訂正＋amend；executor friction 2 條良性（複合指令權限拆單行；新 When 正則為既有帶寬限期 When 的前綴子字串，主動反查 Reqnroll 無 ambiguous match）（114.3K＋amend 輪／61 calls／6.7 分）。orchestrator 親驗後放行 push（pre-push full gate 綠）
- **hook.py 新增 `status=` 賦值攔截 — 事故驅動機械化（矩陣 23c）** — 觸發：`read-only variable: status` 於 active lesson 注入下仍復發（2026-07-10 兩起＋2026-07-12 triage REPEAT ×2）＝習慣承載失效；屬既有 `pre-tool-bash` write/quoting guard 的 pattern 擴充非新制度形態。`_ZSH_STATUS_ASSIGN` regex（命令位置＋`export/local/typeset/readonly` 前綴，masked 字串上跑故引號內文不誤報）；先紅證缺口（3 紅案例改動前 exit 0）→ 落 pattern 後紅案例全 exit 2、5 綠案例全 exit 0（誤擋反查含嵌套引號 payload 以 json.load 反查組裝）；矩陣 23c 列＋lesson 落地欄同 commit（rule 2 restore-trap 紀律未機械化，lesson 誠實維持 active）— `d5b294a`。executor 零 blocker（65.0K／32 calls／4.3 分）；orchestrator 親驗後放行 push（full gate 綠）
- **scenario「同 Consumer + Environment 下已有 Rotating 金鑰 — 拒絕輪替」真 Red→Green，38/48 — INV-4 guard 兌現** — production 五件：`IApiKeyRepository.ExistsRotatingAsync`（鏡射 ExistsNameAsync 形狀含 tenant 範圍）＋Infrastructure 實作＋`RotateKeyFailureCodes.RotationInProgress`（rotate 首個專屬碼）＋handler guard 4（status 之後，零 I/O guards 先行 rationale 刻註解；expired deferral 註解同步收斂）＋`ApiProblem.Map` 首註冊該碼。test：新 Given seed key-C＋匿名 successor（INV-2 成對，CurrentValue 繞 private setter 鏡射先例）＋對照表補條目。真紅 A（恰 1 undefined）＋真紅 B（409 斷言收 200）→ 綠 43/10，回歸紅線三場景綠證，coverage 96.3% — `4562e0a`。**實質發現（executor 追根因）**：spec 的「`+` 一律字面」指示在含 `(.*)` 的 pattern 上反而 undefined——Reqnroll `CucumberExpressionDetector` 因捕獲群組把 pattern 判為 Regex（須 `\+`），與 34 輪無捕獲群組案例（Cucumber Expression，字面 `+`）情境相反；已立 lesson `20260712-reqnroll-plus-escaping-depends-on-pattern-kind.md` 取代過度概括通則。executor 其餘 friction 2 條良性（複合指令拆單行、`rm -rf` 改 mkdir）（176.5K／94 calls／16.7 分）。orchestrator 親驗 scope／trailer／semantic review 後放行 push（pre-push full gate 綠）
- **scenario「金鑰已到期 — 拒絕輪替」真 Red→Green，39/48 — expired guard 兌現、C2 RotateKey 收官（05 檔非 C9 場景全綠）** — 派工前勘查閉合：`ApiKey.Create` 讀宣告求證**無過去時間 guard**（ExpiresAt 直接賦值）→ 過期 seed 直接 `Create` 傳 `now.AddDays(-1)`（now 取 DI TimeProvider，與 handler guard 同凍結 instant），免 CurrentValue 繞法；api-spec §3.2.4 `KEY_ALREADY_EXPIRED | 409` 為既有列，本輪 api-spec 零改動。production 三件：`RotateKeyFailureCodes.KeyAlreadyExpired`（rotate 第二專屬碼）＋handler guard 4 插於 status 與 INV-4 之間（§6.2 順序；`now` 上提供 guard 與 InitiateRotation 共用單次讀鐘，後續步驟編號連動、兩處 deferral 註解就地兌現）＋`ApiProblem.Map` 註冊 409。test：新 Given 過期 seed 變體（鏡射 owned-by-other-consumer 再宣告先例）＋對照表補條目（註解正確寫「既有列」，未重蹈 37/48 退回錯誤）。真紅 A（恰 1 undefined Given）＋真紅 B（409 斷言收 200）→ 綠 44/9，六個 rotate 場景逐場景綠證，Architecture.Tests 14/14，`RotateKeyHandler` coverage 96.4% — `b9b3939`。executor 零 blocker、**零 friction**（自報一次到位；115.3K tokens／61 calls／5.3 分，standard-code 路由）；Refactor-assessment：production 四道 guard 形狀各異判「無」；test 側 `ApiKey.Create` 八參數 seed 樣板第 3 次出現判「值得」——**test-only 重構 pass 候選已累積三項**（System-token 三行樣板 ×5、PostAsync null body ×2、seed 再宣告 ×3），可獨立成輪。orchestrator 親驗 scope／trailer／semantic review 後放行 push（pre-push full gate 綠）
- **Test-only 重構 pass 三家族（2026-07-12 使用者指示「先重構吧」）** — 三獨立 commit 各自綠證 44/9、production diff（`backend/src/`）全程為空：家族 A `AuthenticateAs` extension（9 處 token 樣板，`8f2c201`）；家族 B `PostNoBodyAndCaptureAsync`（3 處無 body POST，`8b2a08d`）；家族 C `ApiKeySeeding.AddSeedKey`（RevokeKeySteps private `CreateSeedKey` 升格共用＋RotateKeySteps 4 處，successor `register: false`，consumerId／expiresAt 變體與 CurrentValue 繞法全留 call site，`00e7b38`）。裁決紀錄：帶 body 的 `PostAsJsonAsync` 各點不抽（body 是 call site 語意）；`CreateApiKeySteps` 兩處 seed 不納入（無 alias 登記、命名語意不同族）；新 helper 依 Infrastructure static/extension 慣例，`FunctionalTestContext` 維持純 state bag。**spec 精度事故（orchestrator 側）**：spec 憑部分閱讀列 RevokeKeySteps caller「5 處」實為 7，executor 正確判定屬漏列並全遷（照抄會留孤兒呼叫編譯失敗）——「列舉型敘述（恰 N 處）須 `grep -n` 全檔取證」已補進 `tasks/_templates/executor-spec.md` 背景欄。executor 零 blocker（146.9K tokens／76 calls／9.0 分）。orchestrator 親驗三 commit scope／semantic review（helper 契約、註解保留、孤兒成員清理）後放行 push（pre-push full gate 綠）

## 待驗證

- 一般互動式 Codex session 首次載入或 hook definition 變更後，仍須由使用者在 Codex `/hooks` 檢視並 trust（harness 安全邊界，repo 側無法代辦）。

## 已嘗試且失敗的方法

- sandbox 內首次 `scripts/ci-checks.sh fast` — `dotnet format` 的 restore operation failed；同指令在核可的可連線環境重跑後全綠，屬 sandbox network 限制，非程式失敗。
- machinery 故意紅首次以 zsh 唯讀變數 `status` 接 exit code，後續 restore trap 又拿到空 mode；已 `chmod 755 scripts/agent/hook.py` 恢復並重跑 machinery／fast gate 綠，教訓記於 `tasks/lessons/20260710-zsh-status-readonly-and-restore-trap.md`。
- A2 派工使用 `fork_turns=none` 控制 transcript 成本，但 spec 漏列 active lessons；executor 的規範載入腳本再次使用 zsh 唯讀變數 `status` 而中止一次，監控發現後在編輯前補讀 16 條 active lessons 並改用 `exit_code`。根因與做法記於 `tasks/lessons/20260710-fork-none-must-carry-active-lessons.md`。
- A2 前置 repo 搜尋以 broad `rg` 執行兩次皆被 `exit 137` 終止且零輸出，縮窄範圍仍同；改用 `git grep` 立即取得結果。另 executor 的規範批次輸出曾截斷，改逐檔／小批次重讀補齊，未影響後續實作。
- A2 checkpoint 首次用單一大型 patch 更新時，context 片段把 `gate 綠` 誤寫成 `gate綠`，`apply_patch` 驗證失敗且零部分寫入；改以小 patch 分段套用並逐段 read-back。
- ADR-025 依 doc-coauthoring 優先嘗試 Gemini CLI cross-model review：首次因未啟用 experimental plan mode 在讀檔前退出；移除該 flag 後，服務端回 `IneligibleTierError`（現有 Gemini Code Assist individual client 不再支援），無可用 model list。改走 skill 的三 persona self-review fallback，補強 pilot target 判準、immutable baseline content 與 Phase 1 停止條件。
- ADR-026 首次 commit 的 pre-commit zh-lint 攔下新 ADR 內一處簡體用字；手動 pre-stage `zh-lint.sh` 因只掃 `git ls-files` 未見 untracked ADR，屬 checkpoint 已登記的既知低優先開環觀察。修正後同一 commit 重跑 fast gate 全綠，未新增機制或 lesson。

## 待裁決

- Tessl 擱置項與 zh-lint 掃描範圍觀察已依 ADR-028 遷入 `tasks/todo.md`（觸發制擱置項段／Non-blocking follow-ups 段），仍非阻塞；原裁決紀錄見 `tasks/archive/process-improvement-plan.md` §9.3 D-2 與 §8.3。

## 下一步（每項獨立可中斷；優先序供參，取捨由規格擁有者決定）

1. ~~上游 skill 實作~~ ✅ **2026-07-11 全數完成**：backlog-decomposition（`5b7bf60`）＋domain-discovery（`4edb8ad`）皆落地並通過綠＋故意紅；地圖兩缺口銷案，剩餘 fog（既有兩 skill 驗證設計、Discovery 解凍、方法論整併）見 `tasks/upstream-map.md`，維持觸發制。首次活體使用已於同日發生（hash 題目開場，skill 事實蒐集階段即發現 ADR-017 已裁決而正確終止——見已完成欄）；下一個真新設計任務開場時直接 `/domain-discovery`。
2. **role-management 探索後續（須使用者指示後開始）**：(a) 協調者依 `docs/design/discovery/role-management/adr-topics.md` 起草餘下 ADR——題 1 已兌現（ADR-027，`6009620`），餘題 2（團隊管理者授權 vs ADR-024 角色集）、題 3（Scope Registry 歸屬遷移）、題 4（級聯撤銷一致性，可考慮與 design-doc Q8 併決）；(b) BDD 場景產出需先解凍 Discovery（ADR-022 凍結中，OQ7）；(c) OQ1／OQ2 兩個小 grilling 缺口可於進 requirements-analysis-design 前補問。
3. **ADR-026 Phase 2 Codex custom agents（Phase 0＋1 已於 2026-07-11 完成）**：在 **Codex session** 新增 `.codex/agents/{explorer,executor,reviewer}.toml`（必填三欄；Explorer／Reviewer 綁 read-only `sandbox_mode`）；model／reasoning 綁定須先過 capability-class eval（ADR-026 Rule 6），eval 前省略即繼承 parent＝誠實 role-only。exit gate 活體驗證：三角色 discovery、錯誤 role name fail-loud、read-only 角色故意寫入被拒、capability mode 明確輸出——此驗證只能在 Codex surface 執行，Claude Code session 勿代跑勿宣稱。Phase 3 routing replay 須等 Phase 2。
4. **Agent Engineering Kit Phase 0（未排程，須使用者指示後開始）**：依 ADR-025 Decision §5 盤點現有 hooks／skills／rules／templates／gates，逐項分類 Core／Stack Profile／Project Overlay，並以反向搜尋列出 domain coupling；只產 inventory 與排除清單，不搬檔、不建 kit repo、不實作 installer。Codex adapter 部分須引用 ADR-026 capability audit，不重複判定。
5. **產品主線 Wave 3 續 — 05_RotateKey 場景「寬限期到期 — 自動完成輪替」（40/48，C9 CompleteGracePeriod 新切片開場）**：**切片形狀警示**：C9 依 api-spec §3.1「不暴露的 Command」＋§3.4 矩陣為 **System Agent Job，非 HTTP endpoint**（`| C9: CompleteGracePeriod | — | — | ❌ System Agent Job |`）——When「System Agent 執行寬限期掃描」的觸發面（背景 job／內部服務直呼／test hook）是**本切片第一個待裁決點**，與既有九個 endpoint 切片先例形狀不同，派工前必須：(a) 讀 detailed-design C9 節（guard 順序、掃描語意、事件 wire）＋integration-spec `KeyGracePeriodExpired` 欄位；(b) bdd-progress L39 註記「FakeClock（Wave 5 後段）」——本場景 Given「當前時間已超過 graceDeadline」在 FrozenTimeProvider（建構時凍結）下的可行做法是 seed 端把 GraceDeadline 設為過去值（CurrentValue 繞 private setter，Rotating seed 先例 `RotateKeySteps.GivenOtherRotatingKeyExists`），無須推進時鐘——但須讀 detailed-design 確認掃描判準方向；(c) 場景三個 Then（Revoked＋清 successor/predecessor 關聯＋KeyGracePeriodExpired 事件＋快取失效）中「觸發主動快取失效」的落點（I 合約？本波是否有消費者？）須查 integration-spec，無消費者時依 JwtBearer lesson 明記首個驗證點；(d) 架構歸屬（Job 放哪個專案、DI 註冊、Architecture.Tests 邊界）可能需要使用者裁決或 ADR——**發現架構級決策時停下來問，勿自行假設**。場景位置：05_RotateKey.feature line 48 `@ignore`（本體 49–56）。基線：FunctionalTests 44 passed/9 skipped（= 39 場景＋5 hasher）。Reqnroll attribute 跳脫判型見 lesson `20260712-reqnroll-plus-escaping-depends-on-pattern-kind.md`。spec 精度注意（累積）：測試計數勿外推（48 場景＋5 hasher）、回歸紅線逐場景綠證一律 `--logger "console;verbosity=detailed"` 併 grep（預設 verbosity 不印 passed 場景名）、migration 需 `dotnet tool restore` 前置、帳面更新排在 ci-checks 之前、scratchpad 訊息檔名帶場景代號、復用既有 Then 前逐一核對該 step 讀取的 response 欄位在新 wire 形狀下存在、When token 依場景措辭選（`TestTokenFactory`，預設 SecurityAdmin）、spec 顯式列 active lessons 讀取義務並標明 active 總數、全量 dotnet test 取證輸出以 `grep -E "<目標場景名>|測試數總計"` 過濾（紅證保留斷言失敗原文，勿全量 detailed dump 回傳）、Read 檔案前先 ls 確認非目錄（EISDIR 家族三見）、列舉型敘述（恰 N 處）須 `grep -n` 全檔取證（模板背景欄已載）。seed 建立一律用 `ApiKeySeeding.AddSeedKey`（重構 pass `00e7b38` 起）、token 發放用 `_ctx.AuthenticateAs(...)`、無 body POST 用 `_ctx.PostNoBodyAndCaptureAsync(...)`。
6. **validation slice 前置合約已備**（ADR-017 Implementation Rule 6）：落地時必須帶 KeyHash 唯一索引 migration、`FixedTimeEquals` 複核、效能 smoke（P99 < 50ms／≥100 RPS）並同 commit 登記矩陣 — 效能無防線區在該點消除。todo #7 併發 guard 仍開放。
7. **小項**：todo #14–#18、#21–#24 housekeeping。
8. **觸發制（勿提前實作）**：checkpoint 分流（`tasks/checkpoints/<workstream>.md`）與 bdd-progress 帳面生成化，規格已定於 ADR-021 §2／§3，觸發條件「第二個常設寫者出現」成立時依規格執行，不需新開 ADR；其餘 CI 端 trailer 覆核／Discovery 解凍見 `tasks/todo.md`「觸發制擱置項」段。

## 工作區狀態警告

> 本欄只留最近 3 輪 triage 紀錄＋常設警告；更早 triage 紀錄見 git log（各輪 checkpoint 落帳 commit）。

- 2026-07-12 重構 pass 收尾 failure triage：五 REPEAT 簽名計數持平；`EISDIR` 家族出現姊妹簽名 ×1（executor 對 `backend/tests/…` 目錄呼叫 Read，路徑異於既有 ×2 簽名）——依 34/46 輪既定處置「再增則入 spec 累積注意」，「Read 檔案前先 ls 確認非目錄」已補入下一步 5 累積注意；lessons active=19 < 20 未觸發。ADR-028 巡帳：todo 無本輪新結案項。
- 2026-07-12 場景 39/48 收尾 failure triage：五 REPEAT 簽名計數均與上輪持平（`== not found` ×4、`Exit code` ×3、`cd backend` ×2、`status=` ×2、`EISDIR` ×2），無新 REPEAT；lessons active=19 < 20 未觸發 triage。ADR-028 巡帳：todo 無本輪新結案項。
- 2026-07-12 場景 38/48 收尾 failure triage：五 REPEAT 簽名計數均未增（`status=` 維持 ×2＝hook 上線前的歷史發生，防線已接管；`EISDIR` 維持 ×2 續觀察），無新 REPEAT。**lessons triage 執行**（Reqnroll lesson 新增使 active=20 觸發）：歸檔 `20260712-ignore-grep-lexicographic-sort-trap.md`（rule 1 已由 CLAUDE.md 自動載入＋bdd-progress.md SSOT 雙重承載、drift 根因已除；rule 2 屬「執行期值求證」既有習慣家族，ADR-013 (b) 判準）→ active=19。ADR-028 巡帳：todo 無本輪新結案項。
- 2026-07-10 A2 push 的 GitHub CI run `29094908277` 成功（build-test 1m16s），但產生非阻塞 annotation：`actions/checkout@v4`、`actions/setup-dotnet@v4` target Node.js 20，runner 已強制使用 Node.js 24。A2 不擴 scope 升版；列為後續 housekeeping 訊號，若 action 警告持續或升為失敗再處置。
- `.agents/skills/tessl__*`、`.tessl/`、`tessl.json`、`.claude/parked/`：既有 Tessl 相關 untracked 項，依 `tasks/archive/process-improvement-plan.md` §9.3 D-2 裁決維持 untracked，不要 `git add`。2026-07-10 起 tessl MCP（原 `.mcp.json`）與 5 個 `tessl__*` skills 收納至 `.claude/parked/`（降低 session 初始載入）。
- 目錄歸檔（Tessl 相關 skill 目錄、`docs/arch-flow.html` 等可重產產物）另包處理，不在本檔範圍內處理。

## 如何接上

> 2026-07-12 起協調者由其他具備 Orchestrator 角色能力的常設模型接任（Fable 5 退場）——憲章 §1 明文規則 (ii) 保證本節流程不因模型更替而變；使用者側的操作方式見 `docs/user-guide.md`（被問「怎麼用」時指過去，勿臨場重寫）。

新 session 直接在 `main` 上工作：讀本檔即知全貌；`docs/orchestration.md` 是協調憲章，ADR 總覽見 `docs/adr/README.md` 索引，`tasks/archive/process-improvement-plan.md` §1–§9 是歷史盤點紀錄（非必讀）。Claude Code／Codex 由各自 config 呼叫 `scripts/agent/hook.py` `session-context`，自動注入 must-read 與 `tasks/lessons/` active 教訓；Codex hook hash 變更後先在 `/hooks` trust。每條新檢驗記得「綠＋故意紅」；phase 收尾更新本檔前先跑 `scripts/failure-triage.sh` 並處置 REPEAT，**同一時機巡活帳面（`tasks/todo.md` 為主）把已結案項移至 `tasks/archive/`（ADR-028 決策 §2）**；報表末行同時判定 lessons triage 門檻（active ≥ 20 即依 `tasks/todo.md` 常設觸發條款執行 lessons triage）。取證輸出衛生（2026-07-12 token 分析裁決）：review／診斷用 `git show --format=` 或 `git diff` 避免重印 commit message；`failure-triage.sh` 每輪只跑一次並以 grep 直取 REPEAT 與 lessons 判定行；本檔「已完成」欄維持最近 ~10 項、triage 紀錄維持最近 3 輪，落帳時同步汰舊。
