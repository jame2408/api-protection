# Checkpoint

> 唯一續接入口（`docs/adr/adr-013-content-tiering-and-injection-slimming.md` 決策 (c)）。欄位比照 `tasks/_templates/checkpoint.md`；歷史交接紀錄見 git log（本檔內容取代 `tasks/archive/process-improvement-plan.md` §8.5 原文）。新 session 直接讀本檔即可接手，不需要先讀 plan 全文。

---

## 分支

`main` only（TBD 轉換已完成；分支紀律見 `docs/adr/adr-012-charter-amendments-external-adoption.md` 決策 (e)）。

## 已完成（含 commit hash）

> 本欄依「如何接上」條文只保留最近數項；更早紀錄（TBD 轉換、Phase I–K、Wave 1 全 10 場景、Wave 2 全 7 場景、ADR-012～026 落地、防線機械化各包、上游 skill charting 與落地、lessons triage 一～四輪、Fable 5 交接包）見 git log 與 `tasks/archive/`。

- **`.claude/agents/` 三角色重啟後活體驗證通過（2026-07-11，session 開場待驗證項銷案）** — discovery：explorer／executor／reviewer 均出現在新 session 的 Agent tool 可用清單（機械觀察）；explorer 寫檔故意紅：Write 遭 allowlist 機械拒絕（`Write exists but is not enabled in this context`）、Edit 不在其工具定義、probe 檔以遞迴搜尋確認未落地。`docs/agent-capability-matrix.md` 缺口登記表銷該列＋Rule 4 段補活體複驗紀錄（與本輪 checkpoint 同 commit）
- **scenario「金鑰非 Suspended 狀態 — 拒絕恢復」Red→Green，26/46** — 純 test-only 啟用（`ResumeKeyHandler` guard 2 本就在）：唯一新 step 為 Then「恢復失敗」（鏡射「暫停失敗」對照表先例，`ProblemAssertions.RequireProblem`；條目 INVALID_STATE_TRANSITION/409＋預鎖下一場景 FORBIDDEN/403）。自然紅（1 undefined step）→ 綠 31/20 → 故意紅（feature seed Locked→Suspended，409 斷言收 200）→ cp 快照還原回綠，production 零改動；`ResumeKeyHandler` coverage 86.7%→93.3%（guard 2 負路徑）— `7c6490c`。executor 零 blocker 零 friction（65.1K tokens／46 calls／3.6 分，standard-code→sonnet 路由，本輪 spec 一次到位零繞路）；orchestrator 親驗 scope／trailer／semantic review 後放行 push（pre-push full gate 綠）
- **`/domain-discovery` 首次完整走通四 Phase — role-management 探索（2026-07-11，全程 HITL）** — 起點為使用者「角色管理」提問；R1–R2 定錨釐清實為 **Data Plane 消費方存取管理**（非 Control Plane 操作者角色）。13 輪 grilling（3 團隊／10 API／每隊 2–3 key 依環境分／季變動／重發制／單租戶裁決／團隊雙面角色／申請→核准／grant 與 key 生命週期分離／收回僅資安）→ event storming（10 事件 6 hotspot，4 個經使用者裁決：修改制加開／級聯撤銷／團隊管理者制／授權不分環境）→ modeling（使用者整包接受：`Team`＋`AccessGrant` 新 aggregate、Consumer 為 Team 使用方投影、新 BC **Team Access**＝Supporting、KeyLifecycle 微擴、Scope Registry 歸屬調整）→ Phase D 五產出落 `docs/design/discovery/role-management/`。DoD：下游 Guardrail 三項中 Context Map ✅、BC 分類 ✅、關鍵 ADR 待協調者依 `adr-topics.md`（4 題）起草。OQ1–OQ7 留 open questions（含 Discovery 解凍屬使用者裁決）
- **ADR-027 Team Access BC 落地形態與 SharedKernel 契約（2026-07-11 使用者「繼續」裁決接受）** — discovery adr-topics 題 1 兌現：獨立 BC `backend/src/TeamAccess/`（登記 `KnownMinimumBoundedContexts`＋故意紅義務）、`IAccessGrantValidator` 同步查詢契約（鏡射 `IConsumerValidator`，ADR-003 §3 模式）、三失敗碼 SharedKernel 單一來源（HTTP 映射延後 api-spec）、`ApiKey` nullable `GrantId`＋既有 `Scopes` 即快照、存量不回填重發自然掛接（OQ4 銷案）。同步項目 design-doc §3.1 補列同 commit — `6009620`；adr-lint 27 檔綠、full gate 綠。orchestrator 親撰（ADR 起草職責）
- **scenario「操作者無恢復權限 — 拒絕恢復」真 Red→Green，27/46 — C6 ResumeKey 收官、03 檔 8 場景全綠** — production：`ResumeKeyEndpoint` bare auth 改 `RequireRole("SecurityAdmin", "TenantAdmin")`，**不含 System**（Resume 無 actor guard、無 422 契約需保護，System 由 policy 擋 403 即正確；與 SuspendKey 先例的差異 rationale 刻在 endpoint 註解）；403 body 沿用 23/46 機制零新增。test：Given 新措辭以 attribute 疊加（零新方法）、Then FORBIDDEN 條目 `7c6490c` 預鎖直接兌現。真紅 A（undefined Given）＋真行為紅 B（403 斷言收 200）→ 綠 32/19，回歸紅線（成功恢復／非 Suspended）detailed verbosity 逐場景綠證，Architecture.Tests 14/14；api-spec §3.2.6 Errors 補 FORBIDDEN 列同 commit — `90feb44`。executor 零 blocker、friction 1 條良性（回歸紅線取證需 `--filter`＋detailed verbosity 才列已通過場景名，取證手法調整）（61.8K tokens／32 calls／4.5 分）；orchestrator 親驗 scope／trailer／semantic review 放行 push（full gate 綠）
- **scenario「System 嘗試暫停 — 拒絕」真 Red→Green，22/46** — 契約缺口先經使用者裁決（api-spec §3.2.5 Errors 補 `HUMAN_ACTOR_REQUIRED`／422，依 L76 總則不以 403 表達）；orchestrator 設計裁決 guard 置頂（零 I/O fail-fast，「System＋不存在 key」回 actor 錯誤屬知情接受）。真 TDD：紅 A（undefined When）＋紅 B（行為紅：422 斷言收 200，production 無 guard）→ production 補 guard（FailureCodes＋Handler 置頂 guard＋ApiProblem 422 映射）→ 綠 27/24；新 When 以 System token＋合法 reason＋Active seed 證明拒絕來自 actor guard；api-spec 表列與帳面同 commit — `9bed11d`。executor 零 blocker 零 friction（101.2K tokens／58 calls／3.4 分，standard-code→sonnet 路由）；orchestrator 親驗 scope／trailer／semantic review 放行。**附帶勘誤**：前輪 21/46 落帳的 Edit 錨點誤吞 KeyCreated 條目標頭（`a30951a`），本 commit 拆回獨立條目
- **scenario「系統偵測到異常 — 自動鎖定金鑰」真 Red→Green，28/46 — 04 LockUnlockKey 新切片開場（C3 LockKey）** — 三契約缺口先經使用者裁決（2026-07-12：走內部端點依 Scanner §3.2.9 先例／ruleId 型別 spec 就場景改 String 依 `3905357` 先例／request body faithful I6 全六欄）。C3 LockKey 垂直切片：`ApiKey.Lock(ruleId, reason, evidence)`＋`KeyLocked(… RuleId: string, Reason, Evidence: JsonElement)`＋Command/Handler 兩 guard（exists→404、非 Active→409，I6 §4.6 三細分碼收斂為單一 INVALID_STATE_TRANSITION 對齊場景語料、rationale 刻在 handler 註解）/內部 Endpoint `POST /internal/keys/{keyId}/lock`。**與 Scanner 先例的關鍵差異**：bare `RequireAuthorization()` 而非 `AllowAnonymous`（「非 System 角色嘗試鎖定」場景需人為請求可達 handler 被拒，System-only 限制留待該場景紅驅動；rationale 刻在 endpoint 註解），When step 帶 System token。FailureCodes 字面值複用、ApiProblem.Map 零新條目（Resume 先例）。文件同 commit：api-spec 新 §3.2.10＋L445 指針＋路由總表列；integration-spec §4.6/§6.1 兩處 `ruleId: UUID→String`（Monitoring 內部事件兩處不動）。真紅 A（3 undefined steps）＋真紅 B（暫緩 Module 註冊，404）→ 綠 33/18，Architecture.Tests 14/14，`LockKeyHandler` coverage 87.5% 自動納管 — `789e562`。executor 零 blocker、friction 1 條良性（CA1305 一次即改用 `GetDateTimeOffset()`）（180.8K tokens／97 calls／11.1 分，standard-code→sonnet 路由）；orchestrator 親驗 scope／trailer／semantic review 後放行 push（full gate 綠）。**Refactor-assessment 兌現 2026-07-11 guard 鏈緩抽裁決**：第 4 個 handler 觸發點評估完成，判不抽（僅 existence check 真同形；status guard 形狀與 guard 順序各異，抽取換得的 indirection 非淨簡化）；test 側 System-token 三行樣板第 4 次出現列為未來 test-only pass 候選
- **ADR-028 知識帳面生命週期落地（2026-07-12 使用者裁決「直接動手做 1–3」）** — 三件一批：(1) `docs/adr/README.md` 索引（28 列）＋adr-lint 檢查 8（索引雙向一致，綠＋雙向故意紅取證）；**附帶修復檢查 6／7 死防線**（`awk | while` subshell 使 violations 計數丟失，違規印紅字卻 exit 0——合成探針證實後改 process substitution，探針復跑 exit 1）；(2) phase 收尾清掃義務正式化（與 failure-triage 同時機，明文不機械化，矩陣無防線區塊登記）＋首次執行（todo.md 已結案四段 → `tasks/archive/todo-closed-2026-07-12.md`）；(3) plan 檔退役 `git mv` → `tasks/archive/process-improvement-plan.md`（殘項遷 todo：zh-lint 掃描範圍觀察、Tessl D-2 擱置），machinery-check 所轄活文件指針同 commit 更新（orchestration §6 治理註記、矩陣 ×3、CLAUDE.md lint 清單、docs/README ADR 段改指針、模板出處註記、machinery-check 註解 ×3）。orchestrator 親作（知識管理裁決＋規格撰寫職責）
- **scenario「金鑰非 Active 狀態 — 拒絕鎖定」Red→Green，29/46** — 純 test-only 啟用（guard 2 本就在 `789e562`）：新 When「System 對 "key-A" 發出鎖定命令」（System token＋faithful I6 六欄合法常值 body，「合法 body＋Suspended seed」隔離證明拒絕來自 status guard，鏡射 22/46 手法）＋新 Then「鎖定失敗」對照表（鏡射「暫停失敗」先例，僅一條 INVALID_STATE_TRANSITION/409；「只有系統可以鎖定金鑰」契約未裁決，註解明文不預鎖）。自然紅（undefined steps）→ 綠 34/17 → 故意紅（feature seed Suspended→Active，409 斷言收 200）→ cp 快照還原回綠，production 零改動；`LockKeyHandler` coverage 87.5%→93.8%（guard 2 負路徑）— `4b91ceb`。executor 零 blocker、friction 1 條良性（`--filter FullyQualifiedName~LockUnlockKey` 不匹配 Reqnroll 產生的測試類名，改跑全量 ~8s 無實質成本）（63.0K tokens／30 calls／3.7 分，standard-code→sonnet 路由）；Refactor-assessment：System-token 三行樣板第 5 次出現，維持留待獨立 test-only pass。orchestrator 親驗 scope／trailer／semantic review 後放行 push（pre-push full gate 綠）
- **scenario「非 System 角色嘗試鎖定 — 拒絕」真 Red→Green，30/46 — C3 LockKey 三場景收官** — 契約缺口經使用者裁決（2026-07-12 選項 (a)）：System-only 以 endpoint role policy 表達 → 403 `FORBIDDEN`（§2.2 既有碼＋`ProblemAuthorizationResultHandler` 既有機制，零新 SharedKernel 碼、零新 handler guard——與 SuspendKey actor-guard 先例的差異 rationale 刻在 endpoint 註解與 commit message）。production：`LockKeyEndpoint` bare auth → `RequireRole("System")`（deferred rationale 註解同步兌現改寫）；test：新 When（SecurityAdmin token＋合法 I6 六欄 body＋Active seed 隔離證明拒絕來自 role policy）＋對照表補 FORBIDDEN/403 條目；api-spec §3.2.10 Authorization／Errors 兩處同 commit。真紅 A（undefined step）＋真行為紅 B（403 斷言收 200）→ 綠 35/16，回歸紅線兩場景（自動鎖定／非 Active 拒絕，System token 過 role policy）逐場景綠證 — `7f02530`。executor 零 blocker 零 friction（57.2K tokens／27 calls／3.4 分，standard-code→sonnet 路由，自報一次到位零繞路）；唯一觀察：自報「讀 5 個 active lesson 全文」與 active=19 有落差（grep 清單有讀、全文擇讀），無輸出品質影響，後續 spec 可顯式標明 active 總數便於覆核。orchestrator 親驗 scope／trailer／semantic review 後放行 push（pre-push full gate 綠）
- **Checkpoint 帳面裁切＋取證輸出衛生（2026-07-12 使用者裁決「執行 1–3 項」，token 節省分析落地）** — (1) 本欄裁至最近 10 項、工作區狀態警告只留最近 3 輪 triage＋常設警告（更早見 git log）；(2) orchestrator 取證衛生入「如何接上」（`git show --format=`／triage 單跑過濾）；(3) 派工 spec 累積注意補「全量 dotnet test 取證輸出過濾」條。executor 側維持現制（spec 精度已使成本降至 57–63K/輪，不併包、不縮 spec）

## 待驗證

- 一般互動式 Codex session 首次載入或 hook definition 變更後，仍須由使用者在 Codex `/hooks` 檢視並 trust（harness 安全邊界，repo 側無法代辦）。

## 已嘗試且失敗的方法

- sandbox 內首次 `scripts/ci-checks.sh fast` — `dotnet format` 的 restore operation failed；同指令在核可的可連線環境重跑後全綠，屬 sandbox network 限制，非程式失敗。
- machinery 故意紅首次以 zsh 唯讀變數 `status` 接 exit code，後續 restore trap 又拿到空 mode；已 `chmod 755 scripts/agent/hook.py` 恢復並重跑 machinery／fast gate 綠，教訓記於 `tasks/lessons/20260710-zsh-status-readonly-and-restore-trap.md`。
- A2 派工使用 `fork_turns=none` 控制 transcript 成本，但 spec 漏列 active lessons；executor 的規範載入腳本再次使用 zsh 唯讀變數 `status` 而中止一次，監控發現後在編輯前補讀 16 條 active lessons 並改用 `exit_code`。根因與做法記於 `tasks/lessons/20260710-fork-none-must-carry-active-lessons.md`。
- A2 前置 repo 搜尋以 broad `rg` 執行兩次皆被 `exit 137` 終止且零輸出，縮窄範圍仍同；改用 `git grep` 立即取得結果。另 executor 的規範批次輸出曾截斷，改逐檔／小批次重讀補齊，未影響後續實作。
- A2 checkpoint 首次用單一大型 patch 更新時，context 片段把 `gate 綠` 誤寫成 `gate綠`，`apply_patch` 驗證失敗且零部分寫入；改以小 patch 分段套用並逐段 read-back。
- ADR-025 依 doc-coauthoring 優先嘗試 Gemini CLI cross-model review：首次因未啟用 experimental plan mode 在讀檔前退出；移除該 flag 後，服務端回 `IneligibleTierError`（現有 Gemini Code Assist individual client 不再支援），無可用 model list。改走 skill 的三 persona self-review fallback，補強 pilot target 判準、immutable baseline content 與 Phase 1 停止條件。
- ADR-026 首次 commit 的 pre-commit zh-lint 攔下新 ADR 內一處簡體用字；手動 pre-stage `zh-lint.sh` 因只掃 `git ls-files` 未見 untracked ADR，屬 checkpoint 已登記的既知低優先開環觀察。修正後同一 commit 重跑 fast gate 全綠，未新增機制或 lesson。

## 待裁決

- Tessl 擱置項與 zh-lint 掃描範圍觀察已依 ADR-028 遷入 `tasks/todo.md`（觸發制擱置項段／Non-blocking follow-ups 段），仍非阻塞；原裁決紀錄見 `tasks/archive/process-improvement-plan.md` §9.3 D-2 與 §8.3。

## 下一步（每項獨立可中斷；優先序供參，取捨由規格擁有者決定）

1. ~~上游 skill 實作~~ ✅ **2026-07-11 全數完成**：backlog-decomposition（`5b7bf60`）＋domain-discovery（`4edb8ad`）皆落地並通過綠＋故意紅；地圖兩缺口銷案，剩餘 fog（既有兩 skill 驗證設計、Discovery 解凍、方法論整併）見 `tasks/upstream-map.md`，維持觸發制。首次活體使用已於同日發生（hash 題目開場，skill 事實蒐集階段即發現 ADR-017 已裁決而正確終止——見已完成欄）；下一個真新設計任務開場時直接 `/domain-discovery`。
2. **role-management 探索後續（須使用者指示後開始）**：(a) 協調者依 `docs/design/discovery/role-management/adr-topics.md` 起草餘下 ADR——題 1 已兌現（ADR-027，`6009620`），餘題 2（團隊管理者授權 vs ADR-024 角色集）、題 3（Scope Registry 歸屬遷移）、題 4（級聯撤銷一致性，可考慮與 design-doc Q8 併決）；(b) BDD 場景產出需先解凍 Discovery（ADR-022 凍結中，OQ7）；(c) OQ1／OQ2 兩個小 grilling 缺口可於進 requirements-analysis-design 前補問。
3. **ADR-026 Phase 2 Codex custom agents（Phase 0＋1 已於 2026-07-11 完成）**：在 **Codex session** 新增 `.codex/agents/{explorer,executor,reviewer}.toml`（必填三欄；Explorer／Reviewer 綁 read-only `sandbox_mode`）；model／reasoning 綁定須先過 capability-class eval（ADR-026 Rule 6），eval 前省略即繼承 parent＝誠實 role-only。exit gate 活體驗證：三角色 discovery、錯誤 role name fail-loud、read-only 角色故意寫入被拒、capability mode 明確輸出——此驗證只能在 Codex surface 執行，Claude Code session 勿代跑勿宣稱。Phase 3 routing replay 須等 Phase 2。
4. **Agent Engineering Kit Phase 0（未排程，須使用者指示後開始）**：依 ADR-025 Decision §5 盤點現有 hooks／skills／rules／templates／gates，逐項分類 Core／Stack Profile／Project Overlay，並以反向搜尋列出 domain coupling；只產 inventory 與排除清單，不搬檔、不建 kit repo、不實作 installer。Codex adapter 部分須引用 ADR-026 capability audit，不重複判定。
5. **產品主線 Wave 3 續 — 04_LockUnlockKey 第四場景「Security Admin 解鎖金鑰」（C4 UnlockKey 新切片開場，派工前須 orchestrator 勘查）**：C4 為對外端點（api-spec §3.2.7 `POST /tenants/{tenantId}/keys/{keyId}/unlock`，response 含 `unlockedBy`），全新垂直切片＝`ApiKey.Unlock(Actor)`＋`KeyUnlocked(keyId, unlockedBy)` 事件＋Command/Handler guard（exists→404、非 Locked→409 見場景 5）/Endpoint＋DI＋Map。勘查點：§3.2.7 request body 有無（鏡射 Resume 無 body 先例？）、Errors 表與場景 5「金鑰非 Locked 狀態」/場景 6「權限不足」的契約對齊、role policy（場景 6 暗示 SecurityAdmin-only 或含 TenantAdmin——對照 §3.2.7 Authorization 欄，可能需使用者裁決）、Given「操作者為 Security Admin」既有（`SuspendKeySteps.cs:29`）。鏡射 ResumeKey 切片先例（`c0e3e1c`：Map 重複註冊陷阱迴避、暫緩 Map/DI 取紅 B）。基線：FunctionalTests 35 passed/16 skipped。spec 精度注意（累積）：測試計數勿外推（46 場景＋5 hasher）、migration 需 `dotnet tool restore` 前置、帳面更新排在 ci-checks 之前、scratchpad 訊息檔名帶場景代號、復用既有 Then 前逐一核對該 step 讀取的 response 欄位在新 wire 形狀下存在、When token 依場景措辭選（`TestTokenFactory`，預設 SecurityAdmin）、spec 顯式列 active lessons 讀取義務並標明 active 總數、全量 dotnet test 取證輸出以 `grep -E "<目標場景名>|測試數總計"` 過濾（紅證保留斷言失敗原文，勿全量 detailed dump 回傳）。
6. **validation slice 前置合約已備**（ADR-017 Implementation Rule 6）：落地時必須帶 KeyHash 唯一索引 migration、`FixedTimeEquals` 複核、效能 smoke（P99 < 50ms／≥100 RPS）並同 commit 登記矩陣 — 效能無防線區在該點消除。todo #7 併發 guard 仍開放。
7. **小項**：todo #14–#18、#21–#24 housekeeping。
8. **觸發制（勿提前實作）**：checkpoint 分流（`tasks/checkpoints/<workstream>.md`）與 bdd-progress 帳面生成化，規格已定於 ADR-021 §2／§3，觸發條件「第二個常設寫者出現」成立時依規格執行，不需新開 ADR；其餘 CI 端 trailer 覆核／Discovery 解凍見 `tasks/todo.md`「觸發制擱置項」段。

## 工作區狀態警告

> 本欄只留最近 3 輪 triage 紀錄＋常設警告；更早 triage 紀錄見 git log（各輪 checkpoint 落帳 commit）。

- 2026-07-12 checkpoint 裁切輪 failure triage：三 REPEAT 仍為既有簽名且計數未增（`== not found` ×4／`Exit code N` ×3／`cd backend` ×2），無新 REPEAT；active=19 < 20 未觸發 lessons triage。
- 2026-07-12 場景 30/46 收尾 failure triage：三 REPEAT 仍為既有簽名且計數未增（`== not found` ×4／`Exit code N` ×3／`cd backend` ×2），無新 REPEAT；active=19 < 20 未觸發 lessons triage。ADR-028 收尾清掃巡帳：todo 無本輪新結案項，無搬移。
- 2026-07-12 場景 29/46 收尾 failure triage：三 REPEAT 仍為既有簽名且計數未增（`== not found` ×4／`Exit code N` ×3／`cd backend` ×2），無新 REPEAT；active=19 < 20 未觸發 lessons triage。ADR-028 收尾清掃巡帳：todo 無本輪新結案項，無搬移。
- 2026-07-10 A2 push 的 GitHub CI run `29094908277` 成功（build-test 1m16s），但產生非阻塞 annotation：`actions/checkout@v4`、`actions/setup-dotnet@v4` target Node.js 20，runner 已強制使用 Node.js 24。A2 不擴 scope 升版；列為後續 housekeeping 訊號，若 action 警告持續或升為失敗再處置。
- `.agents/skills/tessl__*`、`.tessl/`、`tessl.json`、`.claude/parked/`：既有 Tessl 相關 untracked 項，依 `tasks/archive/process-improvement-plan.md` §9.3 D-2 裁決維持 untracked，不要 `git add`。2026-07-10 起 tessl MCP（原 `.mcp.json`）與 5 個 `tessl__*` skills 收納至 `.claude/parked/`（降低 session 初始載入）。
- 目錄歸檔（Tessl 相關 skill 目錄、`docs/arch-flow.html` 等可重產產物）另包處理，不在本檔範圍內處理。

## 如何接上

> 2026-07-12 起協調者由其他具備 Orchestrator 角色能力的常設模型接任（Fable 5 退場）——憲章 §1 明文規則 (ii) 保證本節流程不因模型更替而變；使用者側的操作方式見 `docs/user-guide.md`（被問「怎麼用」時指過去，勿臨場重寫）。

新 session 直接在 `main` 上工作：讀本檔即知全貌；`docs/orchestration.md` 是協調憲章，ADR 總覽見 `docs/adr/README.md` 索引，`tasks/archive/process-improvement-plan.md` §1–§9 是歷史盤點紀錄（非必讀）。Claude Code／Codex 由各自 config 呼叫 `scripts/agent/hook.py` `session-context`，自動注入 must-read 與 `tasks/lessons/` active 教訓；Codex hook hash 變更後先在 `/hooks` trust。每條新檢驗記得「綠＋故意紅」；phase 收尾更新本檔前先跑 `scripts/failure-triage.sh` 並處置 REPEAT，**同一時機巡活帳面（`tasks/todo.md` 為主）把已結案項移至 `tasks/archive/`（ADR-028 決策 §2）**；報表末行同時判定 lessons triage 門檻（active ≥ 20 即依 `tasks/todo.md` 常設觸發條款執行 lessons triage）。取證輸出衛生（2026-07-12 token 分析裁決）：review／診斷用 `git show --format=` 或 `git diff` 避免重印 commit message；`failure-triage.sh` 每輪只跑一次並以 grep 直取 REPEAT 與 lessons 判定行；本檔「已完成」欄維持最近 ~10 項、triage 紀錄維持最近 3 輪，落帳時同步汰舊。
