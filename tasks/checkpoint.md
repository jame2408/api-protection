# Checkpoint

> 唯一續接入口（`docs/adr/adr-013-content-tiering-and-injection-slimming.md` 決策 (c)）。欄位比照 `tasks/_templates/checkpoint.md`；歷史交接紀錄見 git log（本檔內容取代 `tasks/archive/process-improvement-plan.md` §8.5 原文）。新 session 直接讀本檔即可接手，不需要先讀 plan 全文。

---

## 分支

`main` only（TBD 轉換已完成；分支紀律見 `docs/adr/adr-012-charter-amendments-external-adoption.md` 決策 (e)）。

## 已完成（含 commit hash）

> 本欄依「如何接上」條文只保留最近數項；更早紀錄（TBD 轉換、Phase I–K、Wave 1 全 10 場景、Wave 2 全 7 場景、ADR-012～026 落地、防線機械化各包、上游 skill charting 與落地、lessons triage 一～四輪、Fable 5 交接包）見 git log 與 `tasks/archive/`。

- **ADR-030 Discovery 凍結的範圍化解除（2026-07-26 使用者裁決「依 A 起草 ADR-030」）** — **關鍵論證：凍結的前提對 Data Plane 從來就不成立**。凍結句寫「`.feature` scenarios and API specs are already produced」，但實測 Features/ 只有 KeyLifecycle 01–06、backlog 空，而 api-spec §4／§5.6 早已完整定義 `validate-key`——Discovery 當初只跑過 KL。且該凍結敘述是 `be0152e`（規範層衝突修繕）**補寫既有狀態**時加入，未附解除條件，ADR-022 §6 只留「另案處理」。故本 ADR 不解除凍結本身，而是承認前提未涵蓋此類 BC。**解除範圍嚴格限定**：僅 api-spec §4 Data Plane；每條場景須可指回 §4.1 具體條目（7 個 errorCode ＋成功路徑），**規格未定義的行為不得寫成場景，發現缺口須停止回報**；backlog → progress 晉升權**完全不變**；判準（API 契約已定案但場景從未產出）可援引但**不自動適用**，其他 BC 仍須逐案另開 ADR。四個 Alternatives 皆附 Rejected（全面解凍／包裝成 §3 缺陷再現豁免／無限期擱置／下放執行層自行認定）。**同 commit 落兩處同步項目**（CLAUDE.md §BDD 凍結句、`tasks/bdd-backlog.md` 檔頭——兩處原措辭皆為全域禁止，不改會與 ADR 對打），驗收 grep 兩檔各 1 命中已自證。adr-lint 30 檔通過 — 見下方 hash。orchestrator 自行起草。**題 0 就此解除，validation slice 的硬阻斷消失**
- **ADR-029 驗證漏斗執行側與 pepper 邊界（2026-07-26 使用者裁決「依 A 起草 ADR」）** — 裁定**漏斗五層一律在金鑰管理系統內執行**，`POST /api/internal/v1/validate-key` 為唯一觸發面；**pepper 與 `keyHash` 皆不得離開系統邊界**，邊緣快取改存「validate-key 回應（不含 keyHash）」，主動失效機制不變。核心論證：ADR-017 把雜湊改為全域 pepper 的 HMAC 之後，Gateway 側比對必須拿到 pepper → 爆炸半徑由單鑰放大為全體租戶；且 repo 內無 Gateway 產品可整合，該架構無法用場景驅動（違反「無場景不建」）。四個 Alternatives 皆附 Rejected 理由（GW 側原架構／混合分層第 4 層回源／回頭改 per-key salt／不裁定留待實作）。**同 commit 落三處 design-doc 同步項目**（§4.7 補「系統內部投影」註記、§6.2.2 時序圖與雜湊段落重寫為 HMAC＋系統側比對並載明確定性雜湊使 `KeyHash` 唯一索引可直接命中、§8.3 分層快取表 L1 內容改為驗證回應／L2 補「須位於系統信任邊界內」），Status 欄以 Supersedes 標明被取代段落——**題 2（Salt 敘述廢止）就此一併兌現，不另立 ADR**。adr-lint 29 檔通過、fast gate 綠 — 見下方 hash。orchestrator 自行起草（ADR 起草屬協調者職責）
- **validation slice 設計輪（2026-07-26 使用者指示「開 validation slice」→ 協調者建議 B「先設計不寫碼」獲採納）** — 產出 `docs/design/validation-slice-adr-topics.md`（4 題＋1 項流程阻斷，含張力、建議與同步項目；體例鏡射 role-management 的 `adr-topics.md`）。**設計輪的實質發現是三處權威文件互相矛盾**：(1) **漏斗執行側**：api-spec §4.1 寫本系統執行（收 apiKey 回 valid＋metadata），design-doc §6.2.2 時序圖寫 **Gateway 自行執行**（明文「Gateway 將金鑰明文……重新計算雜湊後恆定時間比對」）——**ADR-017 改為全域 pepper 之後，Gateway 側比對等同把全域 pepper 發到每個節點**，爆炸半徑由單鑰變全租戶；建議採 api-spec（本系統側），Gateway 只快取回應不持有 pepper。(2) **design-doc §6.2.2 的 Salt 敘述已被 ADR-017 廢止**：原文寫 per-key salt 與 `$algorithm$salt$hash` 格式，實為 `Base64(HMACSHA256(pepper, rawKey))`、無 salt 且**確定性**——後果不只文字：確定性才可能有 Rule 6(a) 的 `KeyHash` 唯一索引與 O(1) 直查，per-key salt 則須先用 prefix 縮候選再逐筆重算。(3) **效能門檻兩個數字**：api-spec §4「p99 < 5ms」vs CLAUDE.md／ADR-017 Rule 6(c)「P99 < 50ms／≥100 RPS」（矩陣第 99／100 行登記未追蹤並指名本 slice 兌現）——建議 gate 用 50ms、api-spec 的 5ms 改標為設計目標。另題 3（Validation Model 落地形態）建議建 read model 但**第一刀只投影 KL 側欄位**，AP 側留第二刀，並順帶補列 I7 的 `KeyGracePeriodExpired`（40/48 登記的缺口就此可銷案）。**流程阻斷（題 0）**：`Features/` 無驗證場景、backlog 空、`backend/src/` 無 Validation BC，ADR-022 §1 把「新功能（場景不存在）」路由到**凍結中的 Discovery**，§6 又列凍結解除為「另案處理」——**四題全裁決完仍不能派工寫產品碼**，解凍是獨立的流程裁決。commit — 見下方 hash。orchestrator 自行執行（設計裁決與 ADR 起草屬協調者職責，未派 executor）
- **終態濾條零覆蓋缺口收口 — 補兩條場景，50/50（2026-07-26 使用者裁決選項 B「兩條都補」）** — 48/48 輪以 mutation 證實 `GetExpiredNonTerminalAsync` 的兩條終態述詞**皆無場景覆蓋**（整段移除後 53 全綠）；本輪各補一條場景把它們鎖住，**production 零改動**（行為本來就對，缺的是覆蓋）。**路由裁決**：ADR-022 §1 分流表**沒有「既有正確行為的覆蓋缺口」這一列**——不是缺陷、不是行為變更、不是行為移除、也不是 Discovery 新需求；依 §3 豁免精神類推即時新增，但 trailer **誠實寫 `Spec-change: coverage-gap`，不謊稱 defect-repro**（hook 只驗鍵存在）。**同型再出現即為 ADR-022 §1 補列的觸發訊號**（本輪不立案，依制度凍結啟發式）。兩條場景：「已撤銷金鑰即使過期也不重新處理」（Revoked＋已過期→若失守，Status 被 Expired 覆寫、發 KeyExpired，**異常死亡被洗成自然死亡**，安全團隊用 Revoked 篩選再也找不到它——design-doc L722／L731、ADR-03）＋「已到期金鑰不重複發出到期事件」（Expired＋已過期→若失守，**每輪掃描重發一次 KeyExpired**）。**兩個 Given 疊加（終態＋已過期）正是既有六場景構造不出的組合**——48/48 證明不了任何單條防線的根因就是它的 seed 未到期。**兩次精確 mutation 是核心交付**：M1 只移除 `!= Revoked` → **只有場景 1 紅、其餘 49 全綠**；M2 只移除 `!= Expired` → **只有場景 2 紅、其餘 49 全綠**（刻意不兩條一起移除，否則分不出哪條場景守哪條述詞）。test 側 `ThenKeyIsNotInScanResultAndStaysActive` 參數化為 `...StaysAtStatus(alias, expectedStatus)`（`Enum.Parse` 轉型，47/48 場景文字不動且綠證），場景 2 零新 step（復用 48/48 的 Then）。真紅 A（恰 1 undefined）→ 綠 55/0，帳面 48/48 → **50/50**（§3.4 分母遞增，`bdd-lint` 綠）— `4bc8314`。executor 零 blocker、**零 friction**。orchestrator 親驗 scope／trailer 誠實性／兩次 mutation 對應性後放行 push（pre-push full gate 綠）
- **Test-only refactor pass 第二輪，三家族（2026-07-26 使用者指示「先做 test-only refactor pass」）** — 三獨立 commit 各自綠證 53/0、`git diff --stat backend/src/` 跨三 commit 全空。**家族 A**：`InScopedHandlerAsync<THandler, TResult>` 抽入 `TestContextExtensions`（3 處 System Agent Job call site）——**orchestrator 裁決形狀為「收 delegate」而非候選名所暗示的 `ResolveScopedHandler<T>()`**：後者會在 helper return 時 dispose `using var scope`，呼叫端之後才 await 就踩到已釋放 scope，是 bug 不是風格問題；doc-comment 承擔「為何 fresh scope」與「為何收 delegate」，call site 只留場景語意（無 HTTP wire／`_ctx.Response` 為 null）— `da9fbdc`。**家族 B**：`AddSeedKey` 加 `status` 參數（8 處跨 4 檔）——**裁決不抽一行 helper**：8 處共通核心僅一行，真正重複的是「`private set` 繞道」的 rationale 註解，故集中進 `AddSeedKey` doc-comment；4 個純狀態 call site 縮為一行具名參數，4 個複合 call site 保留關聯欄位／`LockRuleId` 的 `Db.Entry` 行並把註解縮到只涵蓋剩餘繞道；參數放最後故既有呼叫端零改動，僅非 Active 才走 CurrentValue（Active 本就是 `Create()` 結果）— `886f498`。**家族 C**：`ThenKeyStatusBecomesExpired` 的 because 子句由「已過期的 **Active** 金鑰」改為「已過期的**非終態**金鑰」（該 step 現服務三場景，45/48 發現的失準）— `07dc704`。家族 B 另附 7 個關鍵場景逐條綠證（動到最多 seed）。ci-checks full 綠、12 個 Handler coverage 全數 OK。executor 零 blocker、friction 1 條（**spec 取證指令 pattern 錯誤**：`--logger "console;verbosity=detailed"` 下摘要格式為 `Total tests:／Passed:` 且 Failed/Skipped 為 0 時整行不印，spec 給的 `grep -E "Passed!|Failed!"` 零命中——該 `Passed!` 行只在不帶 `--logger` 時出現；executor 改用等價 pattern 並註記）。orchestrator 親驗三 commit scope／helper 生命週期正確性／註解搬移後放行 push（pre-push full gate 綠）
- **scenario「金鑰已在終態 — 不處理」真 Red→Green，48/48 — C8 收官、repo 內 `@ignore` 零命中、BDD kanban 佇列清空** — production 零改動；2 新 step（Given Revoked seed 鏡射 `RevokeKeySteps` 三個同型 CurrentValue seed 但落在 `ExpireKeySteps`——C8 掃描場景的 seed 與切片同源，取捨刻註解；Then 只做 step 文字所述兩件：`ProcessedCount == 0` ＋ outbox 無非 KeyCreated 列，**刻意不加狀態斷言**——狀態是 47/48 那條 step 的職責）。真紅 A（恰 2 undefined）→ 綠 **53 passed / 0 skipped**，C8 六場景逐條綠證，`grep -rn "@ignore" Features/` 零命中（exit 1，收官證據），coverage 100% — `3c81ae1`。**雙 mutation 取得的真發現（spec 預測被推翻，executor 如實回報未湊合）**：`GetExpiredNonTerminalAsync` 三條件為 AND，(a) mutation 1 移除日期述詞 → **只有 47/48 紅、48/48 仍綠**（spec 預期兩者皆紅），因 48/48 的 seed 為 Revoked，終態濾條單獨已足以排除它；(b) mutation 2 移除兩個終態排除條 → **53 全綠**，因 48/48 的 seed 未到期，日期述詞單獨已足以排除它。**合併結論：48/48 對任一單條防線都無鑑別力**——它只鎖住「兩條件的合取」，任一條被移除都不會紅；**終態濾條至今仍無場景覆蓋**（mutation 2 是該缺口的機械證據），此登記未因本場景解除，disclosure 已逐字刻在新 Then 的註解與 `ApiKeyRepository` 既有註解。executor 零 blocker、friction 3 條良性（初稿同步 `Any` 改 `AnyAsync` 自行修正；未用的 `keyAlias` 參數加 `_ =` 並註解；如實回報 mutation 1 結果與 spec 不符及其根因）。orchestrator 親驗 scope／trailer／semantic review 後放行 push（pre-push full gate 綠）
- **scenario「金鑰尚未到期 — 不處理」真 Red→Green，47/48 — C8 首個負向場景、掃描結果通道打通** — production 零改動。三項 orchestrator 定案全數落地：(1) 新 Given「尚未超過 expiresAt」**明確設 `now + 1h`，不依賴 seed 預設 +30 天**（否則 seed 預設一改就靜默失效；鏡射 C9 的 future 值 Given）；(2) **同檔抽 `SetExpiresAtRelativeToNowAsync`**（兩 Given 僅差 offset 正負號，第二次出現即抽，鏡射 41/48 已接受的同型抽取，trailer 留痕＋抽後全量複驗綠）；(3) `WhenSystemAgentRunsExpiryScan` 擴充為把 Result 存入 binding 欄位（保留原 `IsSuccess` 斷言，欄位為純 carry-through），打通 `ProcessedCount` 到 Then 的通道——C8 前四個場景只觀察 DB／outbox，本場景起需要「掃描結果本身」。新 Then 三件機械定義：`ProcessedCount == 0`（**本場景相對前四個場景的新增鑑別力**，DB 只有一把 key 故為精確斷言）＋Status 仍 Active＋outbox 無非 KeyCreated 列。真紅 A（恰 2 undefined）→ 綠 52/1 → 故意紅（**移除**查詢的日期述詞而非反轉——反轉會讓 43–46 全落選一起紅；移除後未到期者被誤處理，實測 `ProcessedCount` 收 1 期望 0，**47 紅而 43／44／45／46 全綠**）→ cp 快照還原（md5 一致、`git diff --stat backend/src/` 空）回綠，coverage 100% — `d9a42e3`。**開場異常（重要）**：前一次同任務派工被使用者中斷，但**中斷前已留下部分寫入**（`@ignore` 已移除、Given／helper／When 已在，唯獨新 Then 未落地）；orchestrator 當時未跑 `git status` 即向使用者宣稱「一步都沒跑、工作區乾淨」＝**否定斷言未取證**，已追加進 lesson `20260704-verify-nonexistence-claims.md`（工作區斷言條款＋中斷副作用案例）。executor 正確處置：先 `git stash push`（label `preexisting-wip-47of48-before-executor-run`）取乾淨基線重跑步驟 1–2，才得到可信的紅 A 計數，再取回內容並補上缺漏的 Then。**該 stash 仍留在清單中**（內容已被本 commit 完整吸收，executor 依 Git Safety Protocol 未自行 drop）——待使用者決定 drop 或保留。executor 另 2 條 friction：套用 stash 後首跑意外 1 failed（追出 WIP 漏了新 Then）；首次 Edit 移除 `@ignore` 時 `old_string` 未含該行本身、又多加了規格外註解，以 `git diff` 自檢後修正為單行刪除。orchestrator 親驗 scope／trailer／semantic review 後放行 push（pre-push full gate 綠）
- **scenario「Locked 金鑰到期 — 轉為 Revoked」真 Red→Green，46/48 — C8 唯一非對稱轉移、三項前置一次收口** — 使用者 2026-07-26 照建議定案三項裁決並同輪兌現：(1) `ApiKey.LockRuleId`（nullable，`Lock()` 寫入、`Unlock()` 清除——解鎖即安全上下文結束，rationale 刻 doc-comment）＋migration `AddLockRuleIdColumn`（實測僅一欄 `character varying(200)` nullable）＋`ApiKeyConfiguration` 一行；(2) reason 取 detailed-design C8 版 `"System: locked key expired (original lock rule: {ruleId})"`，同 commit 修 design-doc T10（**執行中發現該字串在 design-doc 有兩處落點，§5 不變條件表早已是目標格式，只 T10 表格用舊版**——spec 的「grep 確認落點唯一」指令與實況不符，executor 正確只改該一行）；(3) 終態濾條無場景覆蓋維持登記、查詢零改動。**分派實作**：`Expire()` 的 Locked 分支**委派 `Revoke(reason, ExpiryScanActor)`** 而非就地賦值（Revoke 已封裝 previousStatus capture＋清 SuccessorKeyId＋發 KeyRevoked，重寫會造出第二個 Revoked 轉移實作）；`LockRuleId ?? "unknown"` 覆蓋 migration 前既有 Locked 列（INV-7「撤銷必須有因」寧可可讀也不 NRE）；`ExpiryScanActor` 為 aggregate 內 `private static readonly` 常數（design-doc T10 規定 reason 與執行者皆系統固定值、非呼叫端選擇，故 `Expire` 簽章不變），命名鏡射 secret-scanner 先例；43/48 的 deferral 註解就地改寫為現況描述，未留過期預告。test：3 新 step（Locked＋ruleId 的 Given 以 CurrentValue 設兩欄、**刻意不呼叫 `key.Lock()`** 以免多發 KeyLocked 進 outbox，ruleId 存 binding 欄位鏡射 `_leakedPrefix`；Then 狀態變 Revoked；Then reason `Contain` ruleId）＋**既有 step 的無 wire 分支改造**：`RevokeKeySteps.ThenActiveCacheInvalidationIsTriggered` 原硬寫 `KeyGracePeriodExpired` 並自帶「future second caller 需更好 discriminator」預告——本場景即該第二呼叫者，改為集合成員檢查（`KeyGracePeriodExpired`＋`KeyRevoked`），註解寫明本 step 定位為「快取失效詮釋紀錄」（精確事件由各場景前一個 Then 鎖定）、**I7 缺口仍未解**、預告已兌現；HTTP wire 分支一行未動。真紅 A（恰 3 undefined）→ 綠 51/2 → 故意紅（Locked 分支條件改 `if (false && ...)`，狀態收 Expired 而非 Revoked，43／44／45 全綠）→ cp 快照還原（md5 一致）回綠；回歸 11 場景逐場景綠證（含 C9 寬限期到期與全部 Lock／Unlock），Architecture.Tests 14/14，coverage 100% — `89e81af`。executor 零 blocker、friction 1 條（即上述 design-doc 落點非唯一）。orchestrator 親驗 scope／trailer／semantic review 後放行 push（pre-push full gate 綠）
- **scenario「Suspended 金鑰到期」Red→Green，45/48 — 純 test-only 啟用、零新 step** — 五 step 全既有（Given 落 `RevokeKeySteps.cs:77` `GivenKeyIsSuspended`：只 seed 一把 key、`CurrentValue` 設 Status、不設 reason 也不建關聯，與 44/48 的 Rotating Given 形狀不同），移除 `@ignore` 即綠 → 故意紅為核心交付。**mutation 依 44/48 建立的紀律再進一步**：改動 `GetExpiredNonTerminalAsync` 的 Where 加 `&& k.Status != ApiKeyStatus.Suspended`（讓掃描漏掉 Suspended）——而非動 `Expire()` 的 `PreviousStatus`（該處任何硬寫常值都會連累 43 或 44，因是服務所有到期場景的泛型 capture）；實測 **45/48 紅（Status 斷言收 Suspended）且 43/48、44/48 皆綠**，鎖住「Suspended 也在掃描範圍內」這條本場景獨有語意。cp 快照還原（md5 一致、`git diff --stat backend/src/` 空）回綠 50/3，回歸四場景綠證，coverage 100% — `87e7fef`。executor 零 blocker、friction 1 條（`cp` 還原後收到 harness 檔案變更提醒，內容與還原後一致；executor 以 `diff` 對快照複核 exit 0 後正確判定為監控雜訊、未依提醒行事）。diff 僅 2 檔。**順帶發現（test-only pass 候選）**：`ExpireKeySteps.ThenKeyStatusBecomesExpired` 的 because 子句寫死「已過期的 **Active** 金鑰」，該 step 現同時服務 Active／Rotating／Suspended 三場景，訊息已失準（45/48 紅證原文可見）。orchestrator 親驗後放行 push（pre-push full gate 綠）
- **scenario「Active 金鑰到期」真 Red→Green，43/48 — C8 ExpireKey 新切片開場** — **切片形狀 orchestrator 裁決（2026-07-26，權威輸入：detailed-design C8／§6.4、design-doc T5/T7/T10/T13＋ADR-03、integration-spec 投影表與 KeyExpired wire、api-spec §3.4；已 grep 反查 `docs/adr/` 全文無既往裁決，故不開新 ADR）**：(1) **只建 scan handler，刻意不鏡射 C9 兩 handler** — 06 檔場景 5／6 的 Then 明文為「不在**掃描結果**中」，把日期與終態過濾放在查詢側；查詢兩者皆濾後 per-key handler 只剩零 guard 轉呼層，依「無場景不建」不建（rationale 刻在 `ExpireKeyScanHandler` 類別註解，含日後抽取條件）；(2) **狀態分派在 aggregate**（§6.4 明文 `key.expire()`）；(3) 本輪只落 Active→Expired。production 六件：`ApiKey.Expire(now)`（capture previousStatus→Expired→事件；doc-comment 內含 Locked→Revoked 的 deferral 插入位）＋`KeyExpired` 事件（`keyId`／`previousStatus`，ToString 依 ADR-006）＋`GetExpiredNonTerminalAsync(now)`（`ExpiresAt <= now` ＋雙重終態排除，tracked／cross-tenant 鏡射 `GetRotatingAsync`）＋`ExpireKeyScanHandler`（單次讀鐘、逐鍵 Expire＋Update、恆 Ok）＋`ExpireKeyScanResponse(ProcessedCount)`（命名避開 Expired，因 46 輪 Locked 分支產 Revoked）＋DI 一行。test：新檔 `ExpireKeySteps.cs` 恰 4 步（Given expiresAt 相對凍結時鐘偏移鏡射 `SetGraceDeadlineRelativeToNowAsync`；When fresh scope 直呼 scan；Then 狀態＋Then 事件——事件 step 的 keyId 取 `_ctx.SeededKeys` 而非 `ResponseBody`，因無 HTTP wire）。真紅 A（恰 4 undefined）＋行為紅（mutation 掉 `Status = Expired` 賦值，狀態斷言收 Active）→ 綠 48/5，回歸四場景綠證，Architecture.Tests 14/14，`ExpireKeyScanHandler` coverage 100% — `85a68df`。executor 零 blocker、friction 2 條（**步驟順序偏離**：先寫 production 才移 `@ignore` 跑紅 A，致紅 B 需靠 mutation 取得而非自然行為紅——證據效力等價但 TDD 序失守，下輪 spec 須明文「步驟順序不得重排」；還原走 cp 快照法正確）。**三項登記（46 輪處理）**：aggregate 未持久化 lock 的 `ruleId`（`Lock()` 只發事件）→ 需 `LockRuleId` 欄位＋migration；reason 字串 detailed-design C8 版 `(original lock rule: {ruleId})` 與 design-doc T10 版 `(rule: {ruleId})` 不一致 → 取前者並同 commit 修 design-doc；終態濾條**無場景覆蓋**（場景 6 seed 用預設 +30 天 expiresAt，日期濾條先排除它）→ 已刻註解誠實登記，若要覆蓋須依 ADR-022 §3 另開缺陷再現場景。orchestrator 親驗 scope／trailer／semantic review 後放行 push（pre-push full gate 綠）


## 待驗證

- 一般互動式 Codex session 首次載入或 hook definition 變更後，仍須由使用者在 Codex `/hooks` 檢視並 trust（harness 安全邊界，repo 側無法代辦）。
- **`CompleteGracePeriodScanHandler` 尚無 production 排程消費者**（`95f969c` 起；DI 直呼是目前唯一觸發面，BDD 場景即消費者。42/48 起 per-key `ICompleteGracePeriodHandler` 亦有測試側直呼觸發面，production 側同樣無 wrapper）——首個排程消費者（HostedService／cron job）落地時為驗證點：週期觸發、DI scope-per-run、cancellation 傳遞；勿默認「編譯過＝可用」。**`ExpireKeyScanHandler`（`85a68df` 起）同題同狀態**，兩個 System Agent Job 應由同一個排程機制收攏。
- **`CompleteGracePeriodScanHandler.cs` 註解陳述漂移**（executor 42/48 輪判定「有」但依 test-only 範圍未動）：「the deadline predicate lives solely in the per-key handler's guard (deferred this round...)」的 deferral 敘述在 `78ef1b0` 落地 deadline guard 後已過期。屬 production-only 一行註解訂正，併入下一個 production 側改動輪處理，勿單開 commit。

## 已嘗試且失敗的方法

- sandbox 內首次 `scripts/ci-checks.sh fast` — `dotnet format` 的 restore operation failed；同指令在核可的可連線環境重跑後全綠，屬 sandbox network 限制，非程式失敗。
- machinery 故意紅首次以 zsh 唯讀變數 `status` 接 exit code，後續 restore trap 又拿到空 mode；已 `chmod 755 scripts/agent/hook.py` 恢復並重跑 machinery／fast gate 綠，教訓記於 `tasks/lessons/20260710-zsh-status-readonly-and-restore-trap.md`。
- A2 派工使用 `fork_turns=none` 控制 transcript 成本，但 spec 漏列 active lessons；executor 的規範載入腳本再次使用 zsh 唯讀變數 `status` 而中止一次，監控發現後在編輯前補讀 16 條 active lessons 並改用 `exit_code`。根因與做法記於 `tasks/lessons/20260710-fork-none-must-carry-active-lessons.md`。
- A2 前置 repo 搜尋以 broad `rg` 執行兩次皆被 `exit 137` 終止且零輸出，縮窄範圍仍同；改用 `git grep` 立即取得結果。另 executor 的規範批次輸出曾截斷，改逐檔／小批次重讀補齊，未影響後續實作。
- A2 checkpoint 首次用單一大型 patch 更新時，context 片段把 `gate 綠` 誤寫成 `gate綠`，`apply_patch` 驗證失敗且零部分寫入；改以小 patch 分段套用並逐段 read-back。
- ADR-025 依 doc-coauthoring 優先嘗試 Gemini CLI cross-model review：首次因未啟用 experimental plan mode 在讀檔前退出；移除該 flag 後，服務端回 `IneligibleTierError`（現有 Gemini Code Assist individual client 不再支援），無可用 model list。改走 skill 的三 persona self-review fallback，補強 pilot target 判準、immutable baseline content 與 Phase 1 停止條件。
- ADR-026 首次 commit 的 pre-commit zh-lint 攔下新 ADR 內一處簡體用字；手動 pre-stage `zh-lint.sh` 因只掃 `git ls-files` 未見 untracked ADR，屬 checkpoint 已登記的既知低優先開環觀察。修正後同一 commit 重跑 fast gate 全綠，未新增機制或 lesson。

## 待裁決

- Tessl 擱置項與 zh-lint 掃描範圍觀察已依 ADR-028 遷入 `tasks/todo.md`（觸發制擱置項段／Non-blocking follow-ups 段），仍非阻塞；原裁決紀錄見 `tasks/archive/process-improvement-plan.md` §9.3 D-2 與 §8.3。
- **validation slice 剩餘兩項待裁決（設計輪原五題；題 1／2 由 ADR-029、題 0 由 ADR-030 於 2026-07-26 裁定並落地）**：**題 3 Validation Model 落地形態**（建 read model 投影 vs 熱路徑直查主表；建議前者且第一刀只投影 KL 側欄位，順帶補列 I7 的 `KeyGracePeriodExpired` 銷案）／**題 4 效能門檻**（api-spec §4「5ms」vs ADR-017 Rule 6(c)「50ms／100 RPS」；建議 gate 用 50ms、api-spec 措辭改標設計目標——ADR-029 的 Negative/Mitigation 已按此假設書寫，裁決時若翻案需回頭修 ADR-029）。完整張力見 `docs/design/validation-slice-adr-topics.md`。
- **I7 投影表契約缺口（2026-07-12 場景 40/48 發現）**：`context-integration-spec.md` §4.7 投影規則表未列 `KeyGracePeriodExpired`，但 design-doc 輪替時序圖與 detailed-design C9 side effect 均要求該事件觸發 VM 主動快取失效（語意上與 KeyRevoked 同級——寬限期結束＝舊金鑰立即失效）。修訂 integration-spec 屬跨文件契約變更，交使用者裁決補列時機（validation slice 設計前補列即可）；test 側註解已記載不預鎖（`RevokeKeySteps.ThenActiveCacheInvalidationIsTriggered`）。


## 下一步（每項獨立可中斷；優先序供參，取捨由規格擁有者決定）

1. ~~上游 skill 實作~~ ✅ **2026-07-11 全數完成**：backlog-decomposition（`5b7bf60`）＋domain-discovery（`4edb8ad`）皆落地並通過綠＋故意紅；地圖兩缺口銷案，剩餘 fog（既有兩 skill 驗證設計、Discovery 解凍、方法論整併）見 `tasks/upstream-map.md`，維持觸發制。首次活體使用已於同日發生（hash 題目開場，skill 事實蒐集階段即發現 ADR-017 已裁決而正確終止——見已完成欄）；下一個真新設計任務開場時直接 `/domain-discovery`。
2. **role-management 探索後續（須使用者指示後開始）**：(a) 協調者依 `docs/design/discovery/role-management/adr-topics.md` 起草餘下 ADR——題 1 已兌現（ADR-027，`6009620`），餘題 2（團隊管理者授權 vs ADR-024 角色集）、題 3（Scope Registry 歸屬遷移）、題 4（級聯撤銷一致性，可考慮與 design-doc Q8 併決）；(b) BDD 場景產出需先解凍 Discovery（ADR-022 凍結中，OQ7）；(c) OQ1／OQ2 兩個小 grilling 缺口可於進 requirements-analysis-design 前補問。
3. **ADR-026 Phase 2 Codex custom agents（Phase 0＋1 已於 2026-07-11 完成）**：在 **Codex session** 新增 `.codex/agents/{explorer,executor,reviewer}.toml`（必填三欄；Explorer／Reviewer 綁 read-only `sandbox_mode`）；model／reasoning 綁定須先過 capability-class eval（ADR-026 Rule 6），eval 前省略即繼承 parent＝誠實 role-only。exit gate 活體驗證：三角色 discovery、錯誤 role name fail-loud、read-only 角色故意寫入被拒、capability mode 明確輸出——此驗證只能在 Codex surface 執行，Claude Code session 勿代跑勿宣稱。Phase 3 routing replay 須等 Phase 2。
4. **Agent Engineering Kit Phase 0（未排程，須使用者指示後開始）**：依 ADR-025 Decision §5 盤點現有 hooks／skills／rules／templates／gates，逐項分類 Core／Stack Profile／Project Overlay，並以反向搜尋列出 domain coupling；只產 inventory 與排除清單，不搬檔、不建 kit repo、不實作 installer。Codex adapter 部分須引用 ADR-026 capability audit，不重複判定。
5. ~~產品主線 BDD kanban~~ ✅ **2026-07-26 全數收官：48/48 全綠，`grep -rn "@ignore" backend/tests/FunctionalTests/Features/` 零命中，FunctionalTests 53 passed / 0 skipped**。`tasks/bdd-progress.md` 佇列已空——**下一輪不再是「找下一個 `@ignore`」**，方向須由使用者重新裁決。已備妥的三個候選（皆獨立可中斷）：
   (a) ~~test-only refactor pass~~ ✅ **2026-07-26 完成**（三家族三 commit，見已完成欄）——候選清單已清空至只剩「`ExpireKeySteps.cs` 298 行是否拆檔」（48/48 與本輪均判不值得，行數再長須重評）；
   (b) ~~終態濾條覆蓋缺口~~ ✅ **2026-07-26 收口**（使用者裁決選項 B「兩條都補」，各以只移除對應述詞的 mutation 精確證明，見已完成欄）——帳面 50/50；**ADR-022 §1 分流表缺「既有正確行為的覆蓋缺口」一列，同型再出現即為補列觸發訊號**（本輪不立案）；
   (c) **validation slice** — 設計輪已完成（見已完成欄與 `docs/design/validation-slice-adr-topics.md`）；**現在卡在待裁決欄的五題**，其中題 0（Discovery 解凍）是硬阻斷：無場景則無法走 BDD 紅綠，四技術題裁決完也不能派工寫產品碼。裁決後的下一動作依題 1 結果分岔——本系統側 → 場景觀察面是 HTTP 回應（`validate-key` 契約已定於 api-spec §4.1，可直接寫場景）；Gateway 側 → 需先開新 ADR 修改 ADR-017 的 pepper 持有者假設（走其 Rule 8 governance clause）。
   **production 側 refactor 候選**：`ActorType.System` 實例化 2 處（secret-scanner、expiry-scan），第 3 處出現時重評是否上移 SharedKernel；`ExpireKeyScanHandler` 與 `CompleteGracePeriodScanHandler` 的掃描迴圈形狀刻意不同（43/48 已判不抽）；key＋successor 雙聚合兩次 `UpdateAsync` 非原子（40/48 觀察，列 production-only pass 候選）。
   **派工紀律（43/48 事故，44/48 已兌現且零 friction）**：spec 步驟欄須明文「順序不得重排——先移 `@ignore` 取證，再寫 production／做故意紅」。**mutation 選擇紀律（44/48 建立、45/48 深化）**：test-only 啟用輪的故意紅，mutation 點必須挑「只讓本場景紅、姊妹場景仍綠」者，並在回報中同時列出各場景輸出——否則證明的是共用路徑活著，不是本場景的獨立鑑別力。**挑點方法**：優先找「只服務本場景語意」的述詞（如 45/48 在查詢加 `!= Suspended`），避開服務所有姊妹場景的泛型段落（如 `Expire()` 的 previousStatus capture）。
   **派工前置檢查（47/48 事故，必做）**：派工前先跑 `git status --short` 與 `git diff --stat`——**被中斷／失敗的前次派工可能已留下部分寫入**（47/48 首次派工遭中斷，仍留下已移除的 `@ignore` 與半套 steps，且 orchestrator 未取證即宣稱工作區乾淨）。工作區不乾淨時，spec 須指示 executor 先取乾淨基線（`git stash push` 帶 label）再跑紅 A，否則紅 A 的 undefined 計數不可信。條文已入 lesson `20260704-verify-nonexistence-claims.md`。
   **test-only refactor pass 候選（累積，待排獨立輪）**：fresh-scope When 樣板 3 處（候選 `ResolveScopedHandler<T>()`）、「seed 一把 key ＋ CurrentValue 設 Status」的 Given **6 處**（46/48 新 Given 使其再 +1，executor 判已跨抽取門檻）、`ExpireKeySteps.ThenKeyStatusBecomesExpired` 的 because 子句寫死「Active」但現服務三場景（47/48 輪 executor 判屬範圍外既有瑕疵、維持登記）。**production 側候選**：`ActorType.System` 實例化 2 處。**production 側候選**：`ActorType.System` 實例化 2 處（secret-scanner、expiry-scan），executor 判暫不上移 SharedKernel，第 3 處出現時重評。
   **spec 取證指令紀律（46/48 事故）**：spec 寫「grep 確認落點唯一」卻實際有 2 處（design-doc 的 reason 字串，§5 表早已是目標格式）——**凡指示「落點唯一」必須由 orchestrator 先自行 grep 取證後才寫入**，否則 executor 得在「照 spec 只改一處」與「自行判斷改哪處」之間二選一。
   spec 精度注意（累積）：測試計數勿外推（48 場景＋5 hasher）、回歸紅線逐場景綠證一律 `--logger "console;verbosity=detailed"` 併 grep（預設 verbosity 不印 passed 場景名）、**`--filter "FullyQualifiedName~<英文名>"` 對本 repo 無效**（Reqnroll 產生的 Feature class 名是中文，如 `輪替金鑰Feature`；要過濾就用中文 feature 名，否則不加 filter 跑全量再 grep）、**取證 grep 勿用 `head -N` 截斷多條件 OR 輸出**（xUnit 平行輸出交錯會漏抓，42/48 輪實例：紅 A 兩條 undefined 只截到一條；改導全量寫檔再逐段核對）、**`ci-checks.sh fast` 不含 build/test/coverage**（要 Handler coverage 數字必須跑 `full`）、migration 需 `dotnet tool restore` 前置、帳面更新排在 ci-checks 之前、scratchpad 訊息檔名帶場景代號、復用既有 Then 前逐一核對該 step 讀取的 response 欄位在新 wire 形狀下存在、When token 依場景措辭選（`TestTokenFactory`，預設 SecurityAdmin）、spec 顯式列 active lessons 讀取義務並標明 active 總數（計數排除 `_README.md`）、全量 dotnet test 取證輸出以 `grep -E "<目標場景名>|測試數總計"` 過濾（紅證保留斷言失敗原文，勿全量 detailed dump 回傳）、Read 檔案前先 ls 確認非目錄（EISDIR 家族**四見**）、列舉型敘述（恰 N 處）須 `grep -n` 全檔取證（模板背景欄已載）、**測試側對照表的 wire／code 值一律寫字面值不寫 production 常數**（常數漂移才會紅，42/48 退回件）。**測試總結行的 grep pattern 依 logger 而異**：不帶 `--logger` 時是 `Passed!  - Failed: N, Passed: N, ...`；帶 `--logger "console;verbosity=detailed"` 時是 `Total tests: N` / `Passed: N`，且 **Failed／Skipped 為 0 時該兩行完全不印**——spec 寫 `grep -E "Passed!|Failed!"` 配 detailed logger 會零命中（2026-07-26 refactor pass 實證）。環境：Testcontainers 需 Docker daemon 已啟動，基線全滅先查 `docker info`。seed 建立一律用 `ApiKeySeeding.AddSeedKey`（**2026-07-26 起支援 `status:` 具名參數，非 Active 狀態的 seed 一律走它，勿再自行寫 `Db.Entry(...).Property(k => k.Status).CurrentValue`**）、token 發放用 `_ctx.AuthenticateAs(...)`、無 body POST 用 `_ctx.PostNoBodyAndCaptureAsync(...)`、**System Agent Job 的 handler 直呼一律用 `_ctx.InScopedHandlerAsync<THandler, TResult>(h => ...)`**（2026-07-26 起）。Reqnroll attribute 跳脫判型見 lesson `20260712-reqnroll-plus-escaping-depends-on-pattern-kind.md`。
6. **validation slice 前置合約已備**（ADR-017 Implementation Rule 6）：落地時必須帶 KeyHash 唯一索引 migration、`FixedTimeEquals` 複核、效能 smoke（P99 < 50ms／≥100 RPS）並同 commit 登記矩陣 — 效能無防線區在該點消除。todo #7 併發 guard 仍開放。**快取層到期處理設計輸入（2026-07-12 使用者方向，設計時作輸入非預決）**：時間驅動失效在快取寫入當下可預知 → 兩法擇一或併用——(a) 快取攜帶 expiresAt／graceDeadline、讀取時比對；(b) TTL 依 `min(標準TTL, deadline − now)` 縮放（注意：TTL 只能編碼寫入當下已知的 deadline，事後才設定的 graceDeadline 靠 `KeyRotationInitiated` 等 command 事件 push 失效＋重寫）；正確性一律由日期判斷承擔（DB 回源路徑同樣帶日期述詞），不依賴 C9 job 時效——job 只收 Audit 事件與 T6 狀態收斂這類無讀取觸發點的殘差。
7. **小項**：todo #14–#18、#21–#24 housekeeping。
8. **觸發制（勿提前實作）**：checkpoint 分流（`tasks/checkpoints/<workstream>.md`）與 bdd-progress 帳面生成化，規格已定於 ADR-021 §2／§3，觸發條件「第二個常設寫者出現」成立時依規格執行，不需新開 ADR；其餘 CI 端 trailer 覆核／Discovery 解凍見 `tasks/todo.md`「觸發制擱置項」段。

## 工作區狀態警告

> 本欄只留最近 3 輪 triage 紀錄＋常設警告；更早 triage 紀錄見 git log（各輪 checkpoint 落帳 commit）。

- 2026-07-26 終態濾條覆蓋缺口收口 failure triage：五 REPEAT 簽名計數與前輪全數持平（`== not found` ×4、`Exit code` ×3、`EISDIR` ×3、`cd backend` ×2、`status=` ×2），無新 REPEAT；lessons active=19 < 20 未觸發。executor 零 friction。**方法論留痕**：多條 AND 述詞的覆蓋證明，須「一次只移除一條」才分得出哪條測試守哪條防線——一次移除多條只能證明合取被守住（48/48 的教訓）。ADR-028 巡帳：待裁決欄「終態濾條無場景覆蓋」項已結案移除。
- 2026-07-26 test-only refactor pass 收尾 failure triage：五 REPEAT 簽名計數與 48/48 輪全數持平（`== not found` ×4、`Exit code` ×3、`EISDIR` ×3、`cd backend` ×2、`status=` ×2），無新 REPEAT；lessons active=19 < 20 未觸發。本輪 friction 為 spec 的測試總結行 grep pattern 與 detailed logger 格式不符，已入下一步「spec 精度注意」欄。ADR-028 巡帳：todo 無本輪新結案項。
- 2026-07-26 場景 48/48 收官 failure triage：五 REPEAT 簽名計數與同日前六輪全數持平（`== not found` ×4、`Exit code` ×3、`EISDIR` ×3、`cd backend` ×2、`status=` ×2），無新 REPEAT；lessons active=19 < 20 未觸發。本輪 orchestrator spec 對 mutation 1 的結果預測有誤（誤判 48/48 會一併轉紅，實為終態濾條對該 seed 是獨立充分條件）——executor 如實回報未湊合，屬 spec 精度而非執行問題；**AND 連接的多重防線，其單一 mutation 影響面須逐 seed 推演後才寫入 spec**。`git stash@{0}`（47/48 遺留）已依使用者指示 drop。ADR-028 巡帳：todo 無本輪新結案項。
- 2026-07-10 A2 push 的 GitHub CI run `29094908277` 成功（build-test 1m16s），但產生非阻塞 annotation：`actions/checkout@v4`、`actions/setup-dotnet@v4` target Node.js 20，runner 已強制使用 Node.js 24。A2 不擴 scope 升版；列為後續 housekeeping 訊號，若 action 警告持續或升為失敗再處置。
- `.agents/skills/tessl__*`、`.tessl/`、`tessl.json`、`.claude/parked/`：既有 Tessl 相關 untracked 項，依 `tasks/archive/process-improvement-plan.md` §9.3 D-2 裁決維持 untracked，不要 `git add`。2026-07-10 起 tessl MCP（原 `.mcp.json`）與 5 個 `tessl__*` skills 收納至 `.claude/parked/`（降低 session 初始載入）。
- 目錄歸檔（Tessl 相關 skill 目錄、`docs/arch-flow.html` 等可重產產物）另包處理，不在本檔範圍內處理。

## 如何接上

> 2026-07-12 起協調者由其他具備 Orchestrator 角色能力的常設模型接任（Fable 5 退場）——憲章 §1 明文規則 (ii) 保證本節流程不因模型更替而變；使用者側的操作方式見 `docs/user-guide.md`（被問「怎麼用」時指過去，勿臨場重寫）。

新 session 直接在 `main` 上工作：讀本檔即知全貌；`docs/orchestration.md` 是協調憲章，ADR 總覽見 `docs/adr/README.md` 索引，`tasks/archive/process-improvement-plan.md` §1–§9 是歷史盤點紀錄（非必讀）。Claude Code／Codex 由各自 config 呼叫 `scripts/agent/hook.py` `session-context`，自動注入 must-read 與 `tasks/lessons/` active 教訓；Codex hook hash 變更後先在 `/hooks` trust。每條新檢驗記得「綠＋故意紅」；phase 收尾更新本檔前先跑 `scripts/failure-triage.sh` 並處置 REPEAT，**同一時機巡活帳面（`tasks/todo.md` 為主）把已結案項移至 `tasks/archive/`（ADR-028 決策 §2）**；報表末行同時判定 lessons triage 門檻（active ≥ 20 即依 `tasks/todo.md` 常設觸發條款執行 lessons triage）。取證輸出衛生（2026-07-12 token 分析裁決）：review／診斷用 `git show --format=` 或 `git diff` 避免重印 commit message；`failure-triage.sh` 每輪只跑一次並以 grep 直取 REPEAT 與 lessons 判定行；本檔「已完成」欄維持最近 ~10 項、triage 紀錄維持最近 3 輪，落帳時同步汰舊。
