# .NET build outputs
bin/
obj/
out/

# Visual Studio
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates
.vscode/

# JetBrains Rider / IntelliJ
.idea/
*.iml

# NuGet
*.nupkg
*.snupkg
packages/
.nuget/

# Entity Framework
# (migrations are committed; exclude nothing specific here)

# Reqnroll generated files
*.feature.cs

# Test results
TestResults/
*.trx
coverage/
*.coverage
*.coveragexml

# Stryker.NET mutation testing output (QA #2, on-demand, non-gate)
StrykerOutput/

# Python bytecode cache (created by `py_compile` syntax checks in
# shared hook.py / machinery-check.sh — PYTHONDONTWRITEBYTECODE has no
# effect on explicit py_compile invocation, only on import-time caching)
__pycache__/

# Runtime secrets / local overrides
appsettings.*.local.json
*.pfx
*.p12

# Environment
.env
.env.*

# OS
.DS_Store
Thumbs.db

# Claude Code — local settings and unrelated skills
.claude/settings.local.json
.claude/skills/buddy-reroll/
# Hook-generated runtime artifacts (observations + failures)
.claude/*.jsonl
.claude/*.marker
# arch-flow-doc skill 產物與 skill 本體（D-3 裁決：可重產、不入制度）
docs/arch-flow.html
docs/flows.json
.claude/skills/arch-flow-doc/
